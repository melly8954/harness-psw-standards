# harness-psw

> 신규 프로젝트용 하네스 설계서다. 기존 하네스를 대체하기 위해 정리 중이다.
> `(프로젝트별 결정)`은 프로젝트마다 채우는 항목이다 (1.5).

## 목차

```text
0. 개요
   0.1 진입 문서 (CLAUDE.md)
1. 공통 원칙
   1.1 정본과 승인
   1.2 ID 추적 체계
   1.3 문서 크기 기준
   1.4 MUST 키워드 사용 기준
   1.5 프로젝트별 결정 항목
2. 전체 구조
3. 기획
   3.1 진행 흐름
   3.2 REQ 구조와 소유 경계
   3.3 요구사항 유형과 경로
   3.4 기능 REQ 크기 기준
   3.5 REQ 템플릿과 수용 기준
4. 설계
   4.1 설계 관점과 정본
   4.2 코드가 정본인 것
   4.3 설계 순서와 교차 검증
   4.4 화면 시안 형식
   4.5 UI 정본 (프론트 키트)
   4.6 코드 규칙 (conventions.md)
   4.7 백엔드 정본 (백엔드 키트)
5. 구현
   5.1 규칙과 강제 수단
   5.2 완료 정의(DoD)
   5.3 환경변수·시크릿 관리
   5.4 커밋 메시지 규칙
6. 운영·유지보수
7. 피드백 루프
   7.1 4요소
   7.2 단계 사이 루프 규칙
   7.3 승인된 내용의 변경
8. 결정과 미결
   8.1 결정 기록
   8.2 미결사항 (open-question.md)
   8.3 용어집 위치
9. 확장: 하위 에이전트
   9.1 역할과 모델
   9.2 흐름
   9.3 원칙
   9.4 권한 강제
10. 하네스 저장소 구성
   10.1 저장소 구조
   10.2 스킬 목록
   10.3 설치
   10.4 구축 후 확인할 것
   10.5 검사 스크립트
부록 A. 인터뷰 질문 목록
부록 B. 설계 문서별 필수 항목
부록 C. CLAUDE.md 템플릿
```

## 0. 개요

- 에이전트가 요구사항과 규칙 문서를 기준 삼아 기획부터 구현까지 진행하기 위한 최소 하네스 구조를 정의한다
- 적용 범위: 신규 프로젝트
- 단계 흐름: 기획 → 설계 → 구현
  → 운영·유지보수는 범위 밖이다 (6절)
- 문서는 코드가 담지 못하는 것만 둔다. 요구사항(무엇을), 설계 규칙(어떻게 만드는가), 화면 시안이다
  → 테이블·API처럼 코드에 이미 있는 것을 문서로 다시 쓰면 둘이 어긋나고 문서가 틀린 정보가 된다 (4.2)
- 기획과 설계는 단계가 끝날 때까지 피드백 루프를 반복 호출하며 검증한다 (7절)
- 결정은 그 값이 적힌 문서에 날짜·이유와 함께 적고, 미결은 `docs/open-question.md` 한 파일에서 관리한다 (8절)

### 0.1 진입 문서 (`CLAUDE.md`)

- `CLAUDE.md`는 어디를 읽을지 알려주는 가벼운 라우터다. 규칙 본문을 옮겨 적지 않는다
  → 매 대화마다 자동으로 읽히므로, 길면 모든 작업에서 토큰이 낭비된다
- 크기: 약 1k 토큰 이내
- 정본 범위
  - 항상 지켜야 할 핵심 규칙: `CLAUDE.md`
  - 절차 세부: 스킬
  - 프로젝트 내용: `docs/`
- 현재 단계는 적지 않는다
  → 금방 틀린 정보가 된다. 에이전트가 단계 진입 조건(1.1)으로 판단한다
- 구성: 작업별 진입 경로, 로딩 원칙, 핵심 규칙, 명령
- 프레임워크가 만드는 에이전트 안내 파일(예: Next의 `AGENTS.md`)은 지우지 않고, 코드 폴더(예: `frontend/`)에 그대로 둔다. 같은 폴더의 `CLAUDE.md`가 `@AGENTS.md`로 불러온다
  → Next는 `next dev`를 실행할 때마다 안내 문구를 `AGENTS.md`에 다시 쓴다. 이 파일이 없으면 문구를 `CLAUDE.md`에 끼워 넣는다
  → 프레임워크 안내(버전별 문서 위치 등)와 하네스 규칙을 파일로 나눠 유지한다
  → 코드 폴더의 `CLAUDE.md`는 그 폴더 파일을 다룰 때만 읽힌다. 백엔드·문서 작업에 프론트 안내가 섞이지 않는다
  → 적용 방법은 프론트 키트의 프레임워크 절차가 소유한다 (4.5)
- 핵심 규칙에는 위반하면 되돌리기 어렵거나 문서 체계가 무너지는 것만 넣는다
  - MUST: 문서를 `approved`로 바꾸지 않는다. 승인은 사용자만 한다
  - MUST: 하위 단계에서 상위 문서를 고치지 않는다. 해당 단계의 루프를 호출한다
  - MUST: 승인된 내용을 바꿔야 하면 먼저 사용자에게 변경안을 보이고 수락을 받는다 (7.3)
  - MUST: 모르는 것은 추측하지 않고 `docs/open-question.md`에 등록한다
  - MUST: 테스트를 통과시키려고 테스트를 고치지 않는다
  - NEVER: 시크릿 값을 읽거나 출력하거나 커밋하지 않는다
  - MUST: 커밋에 트레일러(`Refs`, `Closes`)를 단다 (5.4)
- 하네스 초기화 스킬이 템플릿(부록 C)으로 생성하고, 경로나 명령이 바뀔 때만 수정한다

## 1. 공통 원칙

### 1.1 정본과 승인

- md를 정본으로 둔다
- 최신 상태만 정본으로 두고, 이력은 git에 둔다 (7.1 참조)
- 하위 문서에서 상위 결정을 변경하지 않는다 (7.2 참조)
- frontmatter 키는 영어로 쓴다 (`id`, `status`, `refs` 등). 값과 본문은 한국어로 쓴다
  → 스크립트가 읽기 쉽다
- 문서 상태값은 2개만 둔다 (frontmatter `status:`)
  - `draft`: 작성 중이거나 수정됨. 에이전트가 바꾼다
  - `approved`: 사용자 승인. 다음 단계의 입력으로 쓸 수 있다
- MUST: `approved`로는 사용자만 바꾼다
  - 승인은 사용자가 직접 `approve.sh <경로>`를 실행해서 한다 (Claude Code 입력창에서는 `! bash .claude/scripts/psw/approve.sh <경로>`)
  - 에이전트(메인 세션 포함)가 `status: approved`를 쓰거나 `approve.sh`를 실행하면 hook(`guard-paths.sh`)이 막는다
  - `approve.sh`는 미정·끊긴 자리표시가 남은 파일을 건너뛴다
- 적용 대상은 REQ(`docs/req/`)와 설계 문서(`docs/design/`)다
  - `docs/open-question.md`, `docs/glossary.md`는 상태를 두지 않는다
- `approved` 문서를 수정하면 `draft`로 되돌리고 다시 승인받는다
- `[OPEN-xxx]`가 남은 문서는 `approved`가 될 수 없다. 해당 미결이 "보류"면 예외다 (8.2)
- 도메인 단위 일괄 승인을 허용한다
- 구현 완료, 대체, 범위 제외는 상태값으로 두지 않는다
  - 구현 진행 현황은 커밋 트레일러(`Refs: FR-xxx`)와 AC 테스트 결과에서 뽑는다
    → 구현할 때마다 REQ 파일을 고치면 요구사항 이력이 오염된다
  - 대체: 파일을 삭제하고 커밋 본문에 경위를 남긴다
  - 범위 제외: 도메인 `README.md` 기능 목록의 우선순위 `won't`로 표현한다
- 단계 진입 조건
  - 설계 착수: 해당 도메인 REQ가 `approved`
  - 구현 착수: 해당 REQ와 관련 설계 문서가 `approved`이고, 프로젝트별 결정 항목(1.5)이 모두 채워짐

### 1.2 ID 추적 체계

- 요구사항과 화면에는 고유 ID를 부여한다
  → 피드백 루프나 변경 시 영향 범위를 ID로 좁힐 수 있다
- REQ는 요구사항 문서 전체를 부르는 이름이고, ID 접두사는 유형별로 나눈다

| 대상 | 형식 | 예 |
|---|---|---|
| 기능 | `FR-<도메인>-NNN` | `FR-ORD-001` |
| 비기능 | `NFR-<영역>-NNN` | `NFR-PERF-001` |
| 보안 | `SEC-<영역>-NNN` | `SEC-AUTH-001` |
| 연동 | `INT-<시스템>-NNN` | `INT-PG-001` |
| 데이터 | `DAT-<영역>-NNN` | `DAT-BACKUP-001` |
| 제약 | `CON-NNN` | `CON-001` |
| 수용 기준 | 요구사항 ID + `AC-n` | `FR-ORD-001 AC-2` |
| 화면 | `SCR-<도메인>-NNN` | `SCR-ORD-001` |
| 상태 | 엔터티.상태 | `Order.PAID` |
| 컴포넌트 | 컴포넌트 이름 | `Button` |
| 미결 | `OPEN-NNN` (자리표시 `[OPEN-NNN]`) | `OPEN-012` |

- 화면은 이름·경로가 바뀔 수 있으므로 별도 ID를 둔다. 상태·컴포넌트는 이름 자체를 ID로 쓴다
- 테이블과 API는 ID를 두지 않는다. 코드가 정본이다 (4.2)
- 도메인 코드와 영역 코드는 `req/README.md` 한 곳에서만 정의한다
- ID는 재사용하지 않는다. 삭제된 ID도 다시 쓰지 않는다
- 참조는 하위 → 상위 단방향으로만 적는다

```text
REQ ← 설계(화면, 아키텍처·보안) ← 테스트·커밋
```

| 위치 | 적는 것 |
|---|---|
| 설계 문서 | 근거 요구사항 ID (예: `refs: [FR-ORD-001, NFR-PERF-001]`) |
| 테스트 | 테스트 이름이나 태그에 `FR-ORD-001 AC-2` |
| 커밋 | 트레일러 `Refs: FR-ORD-001` |

  → 상위 문서에 하위 목록을 적으면 같은 연결이 양쪽에 기록되고, 하위가 바뀔 때마다 상위 이력이 오염된다
  → 역방향은 ID 검색으로 찾는다
- 소스 코드 주석에는 요구사항 ID를 적지 않는다
  → 코드는 자주 바뀌고 주석은 갱신되지 않아 금방 틀린 정보가 된다
- 추적 매트릭스(RTM)는 손으로 관리하지 않고, 위 참조를 스크립트로 모아 필요할 때 생성한다

### 1.3 문서 크기 기준

- 원칙: 문서는 선택적 로딩이 가능한 크기로 유지한다 (전역 규칙 준용)
- 신호: 아래를 넘으면 분리를 "검토"한다. 강제 분리하지 않는다
  - 파일당 약 2k 토큰 (한국어 문서는 줄 수보다 토큰으로 판단)
  - 작업 1개당 읽는 파일 3~5개
    → 파일 단위가 아니라 인덱스·참조 구조를 점검하는 기준이다
- 유형별 수치는 각 템플릿이 소유한다
  - REQ 기능 파일, `_policy`: 신호 기준을 그대로 적용한다
  - 아키텍처, `conventions.md`: 응집성을 우선하고, 넘으면 영역 단위 분할을 검토한다
  - HTML 목업, 자동 생성 파일: 적용하지 않는다
- 분리할 때 응집성을 깨지 않는다
  → 같은 정의(정책 수치, 상태 전이)가 두 파일에 나뉘면 분리하지 않는다

### 1.4 MUST 키워드 사용 기준

- 요구사항 문서에서는 두 축을 구분한다
  - `priority` (MoSCoW): 기능 전체가 이번 범위에 들어가는가 → must / should / could / won't
  - 수용 기준 강도: 기능이 들어갔을 때 해당 기준이 필수인가 → [MUST] / [SHOULD]
- 완료 판정과 연결한다
  - [MUST]가 하나라도 FAIL이면 미완료다
  - [SHOULD]를 충족하지 못하면 완료는 가능하지만 사유를 기록한다
  - MAY는 쓰지 않는다
    → 선택사항은 요구사항이 아니다
- 에이전트 규칙 문서에서는 위반이 곧 결함인 것에만 MUST / NEVER를 쓴다
  → 전부 MUST로 쓰면 에이전트가 우선순위를 구분하지 못한다
  - MUST 항목마다 검증 수단을 연결한다: 도구로 강제할 수 있으면 hook·CI, 없으면 검토자 체크리스트
  - 권장사항은 키워드 없이 "왜"와 함께 적는다
- 표기는 영어 대문자 키워드로 통일한다
  → grep과 검증 스크립트 파싱에 유리하다

### 1.5 프로젝트별 결정 항목

- 기술 스택에 따라 달라지는 항목은 하네스에서 값을 정하지 않고, 채우는 시점과 위치만 정한다
  → 프로젝트마다 새로 채운다

| 항목 | 채우는 시점 | 기록 위치 |
|---|---|---|
| 프론트 키트: 프레임워크, 키트 태그, 테마, 셸 형태·설정 (4.4, 4.5) | 설계 착수 직후, UI 기초 작업 전 | `design/architecture.md` UI 절 |
| 백엔드 키트: 프레임워크, 키트 태그, 선택 헬퍼·모듈 (4.7) | 설계 착수 직후, 백엔드 기초 전 | `design/architecture.md` 백엔드 절 |
| 시크릿 저장소 도구 (5.3) | 구현 착수 전 | `design/architecture.md` 배포 단위 |
| 커밋 검사 도구 (5.4) | 구현 착수 전 | `design/conventions.md` |
| `CLAUDE.md` 명령 (0.1) | 구현 착수 전 | `CLAUDE.md` |

- 각 결정은 기록 위치에 값과 날짜·이유를 함께 적는다 (8.1)
- 구현 착수 전에 모두 채워져 있어야 한다 (1.1)

## 2. 전체 구조

```text
<project>/
  CLAUDE.md                 진입 문서 (0절)
  .claude/                  Claude Code 설정 전용
    skills/psw-*/           하네스 스킬 (10.3)
    agents/                 하위 에이전트 (9.1)
    scripts/psw/            검사 스크립트 (10.3)
    psw-version             설치한 하네스 버전
    settings.json           승인 보호·역할 경로 hook (psw-init)
    psw.conf                프로젝트 설정: 테스트 경로 패턴 (psw-design)
  .githooks/commit-msg      커밋 메시지 검사 (psw-implement)
  docs/
    open-question.md        미정·보류 사항 (8.2)
    glossary.md             용어집 (8.3)
    req/                    요구사항 (3절)
      README.md             개요·범위·제약, 도메인·영역 코드, 전체 적용 목록
      actors.md             역할 목록과 권한 매트릭스
      functional/<domain>/  README.md(기능 목록), _policy.md, <feature>.md
      non-functional/
      security/
      integration/
      data/
    design/                 설계 (4절)
      architecture.md
      security.md
      conventions.md        코드 규칙 (4.6)
      ui/
        ia/                 README.md(색인·메뉴 계층), <domain>.md(화면 목록)
        kit/                목업이 함께 쓰는 파일 (shell.js)
        screens/<domain>/<screen>.html
        ui-rules.md
        components.md
  frontend/                 프론트 코드. 테마 CSS와 컴포넌트 코드 정본 (4.5)
    CLAUDE.md               프레임워크 안내를 불러온다 (0.1, 프레임워크가 만들 때만)
    AGENTS.md               프레임워크가 만드는 에이전트 안내 (0.1, 있을 때만)
    <프레임워크 파일>       경로는 프론트 키트의 프레임워크 절차 (4.5)
  backend/                  백엔드 코드. 키트 골격에서 시작한 코드 (4.7)
    <프레임워크 파일>       경로는 백엔드 키트의 프레임워크 절차 (4.7)
```

- 프론트와 백엔드는 한 저장소에 두고, 코드는 `frontend/`와 `backend/`로 나눈다
  → 문서(`docs/`)는 프로젝트에 한 벌만 둔다. 교차 검증과 FR 작업 폴더(9.2)도 한 저장소를 전제로 한다
  → 코드를 같은 깊이에 두면 루트에는 하네스 파일만 남고, 명령과 CI 경로를 같은 방식으로 적는다
  - 워크스페이스 도구(npm workspaces 등)는 쓰지 않는다. 각 폴더가 독립된 프로젝트다
  - 한쪽만 있는 프로젝트는 그 폴더만 만든다
- `docs/`는 프로젝트 루트에 독립해서 둔다
  → `.claude/`는 도구 설정 전용이다. 문서는 사람, CI, 다른 도구도 읽는다
- `docs/` 바로 아래에는 `req/`, `design/`과 단계를 가리지 않는 두 파일(`open-question.md`, `glossary.md`)만 둔다
- 이 문서의 `req/`, `design/` 경로는 `docs/` 기준 상대 경로다
- `design/`은 설계를 뜻한다 (관습적 명칭)

## 3. 기획

### 3.1 진행 흐름

1. 프로젝트 주제를 제시하고 관련 사례나 레퍼런스를 1~2개 참조한다
2. 프로젝트 방향성을 묻는 인터뷰를 진행하고, 답을 REQ 초안에 바로 적는다
3. REQ의 상세(수용 기준, 정책, 권한, 기준 수치)를 채운다 (3.2~3.5)

- 에이전트는 경로별 `README.md`에서 참조 경로를 받고, 필요한 REQ만 읽는다
- 인터뷰 누락이나 방향 오류를 발견하면 피드백 루프로 되돌린다 (7절)
- 인터뷰 진행 규칙
  - 순서: 방향성 → 행위자 → 기능 → 비기능·보안·연동·데이터 → 제약
    → 기능이 나와야 비기능의 적용 범위가 정해진다
  - 한 번에 3~5개씩 질문한다
    → 한꺼번에 많이 물으면 답이 얕아진다
  - 질문에는 선택지와 추천안을 같이 제시한다
    → 사용자가 기획 전문가가 아니어도 답할 수 있다
  - 모르는 것은 `open-question.md`에 등록하고 다음 질문으로 진행한다
  - 해당 없는 것은 `req/README.md`에 "해당 없음"으로 명시한다
    → 빈칸이 "묻지 않음"인지 "필요 없음"인지 구분된다
  - 레퍼런스 사례의 기능을 후보로 제시하고, 사용자가 채택 여부를 고른다
    → 제외한 기능은 `req/README.md`의 범위 밖에 기록한다
  - 인터뷰 원본은 따로 남기지 않는다. 답은 뜻이 바뀌지 않게 REQ에 옮기고, 경위가 필요한 답은 결정 기록(8.1)으로 날짜·이유를 붙인다
    → 원본과 REQ를 둘 다 두면 어느 쪽이 맞는지 다시 따져야 한다
- 누락 검사
  - 인터뷰 체크리스트의 `[필수]` 항목마다 아래 중 하나가 있는지 확인한다
    - REQ에 반영된 답 (반영한 자리에 체크리스트 ID를 남긴다)
    - `open-question.md` 항목
    - `req/README.md`의 "해당 없음"
  - 셋 다 없으면 누락이다
    → 피드백 루프의 트리거가 된다 (7.1)
- 체크리스트 위치
  - 하네스 저장소에 인터뷰 스킬의 참조 자료로 두고, 프로젝트에 복사하지 않는다
    → 모든 프로젝트가 같은 체크리스트를 쓰고, 프로젝트에는 결과(REQ)만 남는다
  - 질문 목록: `skills/psw-interview/references/checklist.md`
  - 항목마다 ID(`ck-<영역>-<번호>`, 소문자)를 둔다. REQ에 반영한 자리(HTML 주석), 미결 항목, "해당 없음" 표가 이 ID로 항목을 가리킨다
    → 누락 검사를 스크립트로 할 수 있다 (`checklist-coverage.sh`)
    → 소문자로 쓰는 이유: 요구사항 ID(`SEC-` 등) 검색에 걸리지 않게 한다

### 3.2 REQ 구조와 소유 경계

- 요구가 존재한다는 사실과 범위, 그 상세를 모두 `req/`가 소유한다. 별도의 요구사항 정의서(SRS)를 두지 않는다
  → 목록과 상세를 다른 문서에 두면 같은 요구가 두 곳에 적히고 한쪽만 고쳐진다
- 파일별 소유

| 파일 | 소유하는 것 |
|---|---|
| `req/README.md` | 개요(배경, 대상 사용자, 성공 기준, 레퍼런스), 범위 안·밖, 첫 출시(MVP), 해당 없음, 제약사항(`CON-`), 도메인·영역 코드, 전체 적용 요구사항 목록 |
| `req/actors.md` | 역할 목록, 역할별 권한 매트릭스 |
| `req/functional/<domain>/README.md` | 그 도메인의 기능 목록: ID, 한 줄 요구, 우선순위, 파일 |
| `req/functional/<domain>/<feature>.md` | 기능 하나의 상세: 흐름, 예외, 수용 기준 |
| `req/functional/<domain>/_policy.md` | 도메인 공유 규칙·수치, 상태 전이 |
| 영역 파일 (`non-functional/` 등) | 비기능·보안·연동·데이터 항목과 기준 |

- 기능 목록의 한 줄 요구와 FR 파일 제목을 같게 유지한다
- 우선순위는 도메인 `README.md`의 기능 목록만 소유한다. FR 파일에는 적지 않는다
- 개정 방식은 7.1을 따른다

### 3.3 요구사항 유형과 경로

| 유형 | 예시 | 경로 |
|---|---|---|
| 기능 | 로그인, 주문 | `req/functional/<domain>/` |
| 비기능 | 응답시간, 동시접속, 가용성 | `req/non-functional/` |
| 보안 | 인증 방식, 개인정보 마스킹, 감사 로그 | `req/security/` |
| 연동·외부 인터페이스 | 결제 PG 등 외부 API | `req/integration/` |
| 데이터 | 이관, 백업 | `req/data/` |
| 역할·권한 매트릭스 | 역할별 권한 | `req/actors.md` |
| 제약사항 | 기술 스택, 일정, 예산, 법규 | `req/README.md` 제약 절 |

```text
req/
  README.md                          개요·범위·제약, 코드표, 전체 적용 목록
  actors.md
  functional/<domain>/               README.md, _policy.md, <feature>.md
  non-functional/
  security/
  integration/
  data/
```

- 기능 요구사항 1개당 md 1개를 산출한다
  → 기능 요구사항이 130개면 기능 REQ 파일도 130개다
- 역할 목록과 권한 매트릭스는 `req/actors.md`가 소유한다
  - FR 파일의 행위자는 주 흐름을 수행하는 주체만 적는다. 누가 허용되는지는 `actors.md`에만 적는다
  - `actors.md` 형식: 행은 FR(도메인별 절), 열은 역할. 값은 `○`(허용), `-`(불가), `△`(조건부, 조건은 표 아래에 적음)
- 제약사항은 상세가 따로 없으므로 REQ 파일을 두지 않고 `req/README.md` 표에 둔다
- 비기능·보안·연동·데이터는 영역별 파일 1개에 여러 항목을 담는다
  → 항목이 짧고 여러 기능에 걸치므로, 항목마다 파일을 만들면 파일 수만 늘어난다

| 폴더 | 파일 (예) | ID 영역 코드 |
|---|---|---|
| `non-functional/` | `performance.md`, `availability.md`, `compatibility.md`, `accessibility.md` | `PERF`, `AVAIL`, `COMPAT`, `A11Y` |
| `security/` | `authentication.md`, `authorization.md`, `privacy.md`, `audit.md` | `AUTH`, `AUTHZ`, `PRIV`, `AUDIT` |
| `integration/` | `<system>.md` (예: `pg.md`, `sms.md`) | `PG`, `SMS` |
| `data/` | `migration.md`, `retention.md`, `backup.md` | `MIG`, `RET`, `BACKUP` |

- 파일 이름과 영역 코드를 1:1로 맞춘다
  → `NFR-PERF-001`을 보면 `performance.md`에 있다는 것을 바로 알 수 있다
- 영역 코드는 도메인 코드와 함께 `req/README.md`에서만 정의한다 (1.2)
- 파일이 크기 기준(1.3)을 넘으면 영역 안에서 다시 나눈다
- 항목은 표 한 줄에 하나씩 적는다. 상태(`status`)는 파일 단위로 둔다 (1.1)

```markdown
| ID | 요구 | 기준 | 확인 방법 | 적용 범위 | 우선순위 |
|---|---|---|---|---|---|
| NFR-PERF-001 | 주문·결제 API 응답 시간 | p95 ≤ 500ms (동시 500명) | 부하 테스트 | 개별 | must |
| NFR-PERF-002 | 전체 페이지 첫 화면 표시 | LCP ≤ 2.5초 (모바일 4G) | 브라우저 측정 | 전체 | should |
| NFR-PERF-003 | 검색 응답 시간 | [OPEN-031] | 부하 테스트 | 개별 | should |
```

- 기준은 측정할 수 있는 수치로만 적는다. 정해지지 않았으면 `[OPEN-xxx]` 자리표시를 둔다
- 적용 범위는 전체와 개별로 나눈다

| 적용 범위 | 뜻 | 연결 방법 |
|---|---|---|
| 전체 | 모든 기능에 적용 | FR 파일에 적지 않는다. `req/README.md`에 전체 적용 목록으로 모은다. 이 목록은 영역 파일에서 다시 만들고 직접 고치지 않는다 |
| 개별 | 특정 기능에만 적용 | FR 파일의 참조 요구사항 항목에만 적는다 |

  → 같은 연결을 양쪽에 적지 않는다 (1.2 단방향 원칙)
  → 구현자는 FR 파일과 `req/README.md`의 전체 적용 목록만 읽으면 된다

### 3.4 기능 REQ 크기 기준

- 기능 파일과 도메인 정책 파일(`_policy.md`)을 함께 쓴다
  → 잘게 분리하면 공유 규칙(만료 시간 등)이 여러 파일에 중복되고, 수정할 때 빠뜨릴 위험이 있다
  → 하나로 통합하면 읽는 양이 늘고, REQ 1개가 여러 행위를 덮어 추적성이 떨어진다
- 기능 파일 1개 = 사용자가 독립적으로 시작하고 끝내는 행위 1개
- 분리 조건
  - 트리거(행위자, 진입점)가 다를 때
  - 수용 기준을 따로 판정할 수 있을 때
  - 외부 연동이 달라 실패 흐름이 다를 때 (이메일 로그인 / 카카오 로그인)
  - 우선순위나 릴리스 시점이 다를 때
- 정책으로 빼는 조건
  - 사용자 행위가 아니라 규칙·수치일 때 (만료, 제한, 형식)
  - 둘 이상의 기능이 같이 참조할 때
- 정책의 위치
  - 도메인 안에서만 쓰이면 `req/functional/<domain>/_policy.md`
  - 여러 도메인에 걸치면 `req/security/`, `req/non-functional/`로 올린다
- 상태 전이
  - 상태가 3개 이상이거나 시간·외부 이벤트로 상태가 바뀌는 엔터티는 그 도메인 `_policy.md`에 상태 전이 절을 둔다
    → 여러 기능이 같은 상태를 바꾸므로 기능 파일에 나누면 전이 규칙이 어긋난다. 어떤 전이가 허용되는지는 업무 규칙이라 요구사항이 소유한다
  - 구성: 상태, 전이(현재·이벤트·다음·조건·주체·부수 효과·근거 FR), 금지 전이
  - 상태값의 정본은 이 절이다. 코드의 상태 이름은 이 절을 따른다
  - 전이 표의 각 행은 AC 테스트(5.2)의 기준이 된다

```text
req/functional/auth/
  README.md       기능 목록 (ID, 한 줄 요구, 우선순위, 파일)
  _policy.md      세션 만료, 자동 로그인 기간, 동시 로그인 수 (수치의 유일한 정의처)
  login-email.md
  login-social.md
  logout.md
  auto-login.md
```

### 3.5 REQ 템플릿과 수용 기준

- FR 파일 구성
  - frontmatter: `id`, `status`, `refs`(개별 적용 NFR·SEC·INT·DAT, 3.3)
  - 본문: 행위자, 선행조건, 주 흐름, 예외 흐름, 수용 기준, 정책 참조(`_policy.md` 항목)
- 우선순위는 FR 파일에 적지 않는다
  → 도메인 `README.md`가 소유한다 (3.2)
- 수용 기준이 핵심이다
  → 구현 완료 판정(5.2)과 테스트 진행의 기준이 된다
- 수용 기준의 강도 표기는 1.4를 따른다

## 4. 설계

### 4.1 설계 관점과 정본

- 설계 문서는 코드로 옮기기 전에 정해야 하고, 코드만 봐서는 알기 어려운 것만 둔다

| 문서 | 담는 것 |
|---|---|
| `design/architecture.md` | 구성, 기술 스택, 모듈 경계와 의존 방향, UI·백엔드 키트 적용 결과, 외부 연동(호출·실패 처리·테스트 환경), 배포 단위 |
| `design/security.md` | 인증·인가 구조, 데이터 보호, 감사 로그 |
| `design/conventions.md` | 코드 규칙: 백엔드·프론트·DB·REST·UI 코드 (4.6) |
| `design/ui/ia/` | 화면 목록, 메뉴 계층, 화면 흐름 |
| `design/ui/screens/` | 화면 시안 (4.4) |
| `design/ui/ui-rules.md` | 화면을 그릴 때의 규칙 |
| `design/ui/components.md` | 공통 컴포넌트 목록 |

- 보안 설계는 보안 요구사항(`req/security/`)을 받는다
- 연동 설계는 연동 요구사항(`req/integration/`)을 받아 `architecture.md` 외부 연동 절에 둔다
- 설계 문서 공통 규칙
  - frontmatter에 `status`(1.1)와 `refs`(근거 요구사항 ID, 1.2)를 적는다
  - 섹션 순서: 목적·범위 → 본문 → 금지·제약 → 미결 자리표시
  - 다른 곳이 정본인 값은 적지 않고 참조한다

| 값 | 정본 |
|---|---|
| 상태값·상태 전이 | `req/functional/<domain>/_policy.md` 상태 전이 절 (3.4) |
| 테이블·컬럼 | 백엔드 DB 마이그레이션 (4.2) |
| API 경로·요청·응답 | 백엔드 코드와 거기서 만든 API 문서(예: Swagger) (4.2) |
| 에러 코드 목록 | 백엔드 코드의 에러 코드 정의. 형식과 번호 규칙은 `design/conventions.md` REST 절 (4.7) |
| 테마, 컴포넌트 코드 | 프론트 코드 (4.5) |
| 정책 수치 | `req/functional/<domain>/_policy.md` |
| 역할별 권한 | `req/actors.md` |
| 메뉴 | `design/ui/ia/README.md` 메뉴 계층 |

  - HTML 목업은 frontmatter 대신 파일 맨 위 주석에 메타 정보를 적는다
    → 형식: `<!-- psw id: SCR-ORD-001 | refs: FR-ORD-001, FR-ORD-003 | status: draft -->`
  - 목업은 기본, 빈 상태, 로딩, 오류 네 가지 상태를 보여준다
- 문서별 필수 항목과 템플릿: `skills/psw-design/references/`
- 교차 검증 스크립트가 읽는 형식

| 문서 | 형식 |
|---|---|
| `ui/ia/<domain>.md` | 화면 목록 표의 행: `\| SCR-xxx-NNN \| 이름 \| 경로 \| 접근 역할 \| refs \|` |
| 목업 HTML | 맨 위 메타 주석 (위 형식) |
| `ui/components.md` | 컴포넌트 표의 첫 열이 컴포넌트 이름 (예: `Button`). 목업의 `data-component` 값과 같다 |
| 목업 HTML (컴포넌트) | 컴포넌트를 쓴 요소에 `data-component="<컴포넌트 이름>"`. 변형은 `data-variant` |

### 4.2 코드가 정본인 것

- 테이블 설계(ERD), 도메인별 API 명세는 문서로 두지 않는다
  - 테이블·컬럼: DB 마이그레이션 파일이 정본이다. 이름 규칙, 표준 컬럼, 타입, 삭제 방식은 `conventions.md` DB 절이 정한다
  - API: 컨트롤러와 요청·응답 코드가 정본이다. 사람이 볼 문서는 코드에서 만든다 (예: Swagger). 경로·응답 형식·오류 형식 규칙은 `conventions.md` REST 절이 정한다
  → 구현 뒤에는 문서가 코드와 어긋나기만 한다. 공통 규칙만 문서로 두면 도메인이 늘어도 문서가 늘지 않는다
- 구현 전 합의가 필요한 계약(API 경로, 요청·응답 형식, 오류 코드, 테이블)은 FR 구현 첫 단계에서 코드로 먼저 쓴다 (9.2)
  → 검증자가 이 계약으로 AC 테스트를 쓰고, 프론트 작업도 이 계약을 기다린다
- 연결: 계약과 구현 커밋에 `Refs: <FR-ID>` 트레일러를 단다. 코드와 요구사항의 연결은 커밋과 AC 테스트 이름으로 찾는다 (1.2)

### 4.3 설계 순서와 교차 검증

- 아키텍처 → UI 기초(4.5)·백엔드 기초(4.7) → 코드 규칙(4.6) → 화면 순서로 진행한다
  → UI 기초를 만들려면 기술 스택이 `req/README.md` 제약 절에서 먼저 정해져야 한다
- 설계는 기획과 별개로 자체 피드백 루프를 반복한다 (7절)
- 교차 검증 항목

| # | 구분 | 항목 | 검사 방식 |
|---|---|---|---|
| 1 | 누락 | 승인된 `FR` 중 화면에서 참조되지 않는 것 (경고. 서버 전용 기능이면 무시) | 스크립트 |
| 2 | 누락 | 모든 `NFR` / `SEC` / `INT`가 설계 문서 어딘가에서 근거로 참조된다 | 스크립트 |
| 3 | 일치 | IA 접근 역할이 `actors.md`와 일치한다 | 검토자 |
| 4 | 일치 | 목업의 `data-component` 값이 `components.md`에 있다 | 스크립트 |
| 5 | 일치 | 목업마다 IA 화면 목록에 행이 있다. IA 행에 목업이 없으면 경고 | 스크립트 |
| 6 | 고아 | 근거가 비어 있는 `SCR`이 없다 | 스크립트 |
| 7 | 고아 | 존재하지 않는 ID를 참조하지 않는다 | 스크립트 |

- 교차 검증은 설계 루프를 돌 때마다, 그리고 설계 문서를 `approved`로 올리기 전에 실행한다
- 불일치 처리
  - 설계 문제: 설계 문서를 `draft`에서 고친다
  - 요구사항 문제: 기획 루프를 호출한다 (7.2)
  - 근거가 빈 설계 요소: 범위 밖 작업을 의심한다

### 4.4 화면 시안 형식

- 화면 시안은 HTML 목업으로 작성한다
  → 이미지만 두면 에이전트가 읽지 못한다
- 목업은 프론트 키트의 목업 템플릿에서 시작하고, 키트의 목업 절차를 따른다 (4.5)
  - 모양: 키트의 컴포넌트 조각(snippet)을 그대로 쓴다. 조각이 실제 컴포넌트와 같은 모양인지는 키트가 검증한다
  - 표시: 컴포넌트를 쓴 요소에 `data-component`를 단다 (4.1)
  - 스타일 도구, 테마를 연결하는 방법, 목업을 여는 데 필요한 조건(인터넷 연결 등)은 키트가 정한다
- 목업은 `components.md`에 있는 컴포넌트만 쓴다
  → 새 컴포넌트가 필요하면 `components.md`에 먼저 추가한다
- 목업이 함께 쓰는 파일은 `design/ui/kit/`에 둔다
  - 셸(모든 화면을 감싸는 헤더·메뉴 틀)은 프로젝트에 하나(`design/ui/kit/shell.js`)만 두고, 목업은 그 파일을 불러온다
    → 셸을 목업마다 복사하면 메뉴가 바뀔 때 모든 목업을 고쳐야 한다. 실제 앱도 셸을 레이아웃 한 곳에 둔다
  - 테마는 복사하지 않고 프론트 코드의 테마 파일을 직접 연결한다 (4.5)
- 셸
  - 형태: 키트의 셸 틀 중 하나를 고른다. 형태는 HTML 뼈대가 다른 것만 나누고, 위치·접기·생김새 같은 차이는 형태 안의 설정이다
  - 형태와 설정은 결정 기록(8.1)으로 `architecture.md` UI 절에 적는다. 실제 앱의 셸 코드도 같은 설정을 쓴다
  - 메뉴는 `ia/README.md` 메뉴 계층과 같다. 셸 파일은 메뉴를 옮겨 적기만 한다
  - 메뉴를 관리 기능으로 바꾸는 프로젝트는 메뉴 계층이 초기 메뉴를 뜻한다. 그 사실과 근거 FR을 `ia/README.md`에 적는다
  - 셸을 쓰지 않는 화면(로그인 등)은 목업에서 셸을 불러오지 않는다. `ia/README.md`의 셸 없는 화면 목록에 적는다
- IA
  - `ia/README.md`: 도메인 색인, 메뉴 계층, 셸 없는 화면, 화면 흐름
  - `ia/<domain>.md`: 그 도메인의 화면 목록 표 (4.1 형식)
    → 화면이 수십 개가 되면 한 파일이 크기 기준을 넘고, 도메인 작업마다 전체 목록을 읽게 된다
- 승인 시점의 모양은 git에 남은 목업 파일로 본다

### 4.5 UI 정본 (프론트 키트)

- 디자인은 새로 만들지 않고 생태계의 검증된 UI 라이브러리를 쓴다. 하네스는 쓰는 방법만 규범으로 정한다
  → 디자인 전문가 없이도 일정한 품질을 얻는다
- 프론트 키트 저장소: `harness-psw-frontend` (비공개, 태그로 버전 관리)
  - 테마, 목업 템플릿과 컴포넌트 조각, 기본 컴포넌트 목록, 프레임워크별 적용 절차를 둔다
  - 지원하는 프레임워크와 UI 라이브러리, 검증 기록은 키트가 소유한다. 키트에 없는 프레임워크는 키트에 먼저 추가한다
- 하네스와 키트의 경계
  - 하네스: 무엇을 지키는가. 목업 형식(메타 주석, `data-component`, 네 가지 상태), 정본 위치, 공유 파일 권한, 설계 순서, 키트 태그를 결정 기록으로 남기는 것
  - 키트: 어떻게 하는가. 프레임워크별 파일 경로, 도구와 명령, 목업 템플릿 사용법, 태그
  → 프레임워크를 추가하거나 키트 태그를 올려도 하네스를 고치지 않는다
  → 하네스 스크립트(`crosscheck.sh`)가 읽는 형식은 하네스가 소유한다. 키트가 형식을 정하면 키트가 바뀔 때 스크립트가 알리지 않고 깨진다
- 적용 방식: 설계 단계에서 키트 태그와 테마를 골라 결정 기록으로 남기고, 파일을 프로젝트에 복사한다
  - 태그는 가장 최근 태그를 권장한다. 키트는 검증을 마친 커밋에만 태그를 단다
  - 복사한 뒤에는 프로젝트 쪽이 정본이다. 키트를 고쳐도 진행 중인 프로젝트는 바뀌지 않는다
  - 패키지로 설치하지 않는다
    → UI 라이브러리가 컴포넌트 코드를 프로젝트에 복사하는 방식이라 재사용할 것이 설정과 규범뿐이다
- 프로젝트 정본
  - 테마: 테마 변수를 한 파일에만 둔다
    → 한 파일에만 있어야 목업도 같은 파일을 연결할 수 있다
  - 컴포넌트: UI 라이브러리로 추가한 코드
  - 두 경로는 키트의 프레임워크 절차가 정하고, `design/architecture.md` UI 절에 적는다
  - 커스터마이즈는 테마 변수로만 한다. 컴포넌트 코드의 스타일을 직접 바꾸지 않는다
- `design/ui/` 문서에는 값을 적지 않는다
  - `ui-rules.md`: 화면을 그릴 때의 사용 규칙과 이유. 프론트 코드 규칙은 `conventions.md`가 소유한다
  - `components.md`: 컴포넌트 이름 → 코드 경로 → 용도 목록 (키트의 기본 목록에서 시작)

### 4.6 코드 규칙 (`conventions.md`)

- 코드 규칙은 `design/conventions.md` 한 파일에 둔다. 백엔드·프론트·DB·REST와 둘 사이의 연관 규칙을 모두 담는다
  → 공통 API 규칙(경로, 응답·오류 형식, 페이징), DB 규칙(이름, 표준 컬럼, 삭제 방식)이 여기 있어야 도메인별 API·테이블 문서 없이 구현할 수 있다 (4.2)
- 구성
  - 적용 범위, 메타 원칙(규칙 강도 표기, 규칙과 코드가 다를 때 어느 쪽을 고치는가)
  - 백엔드: 패키지 구조, 레이어, 이름, 트랜잭션, 응답·예외, 검증, 설정, 보안
  - DB: 테이블·컬럼 이름, 표준 컬럼, 타입, 인덱스·제약, 마이그레이션, 하드 삭제 예외
  - 프론트: 디렉터리, 컴포넌트, API 호출, 상태 관리, 스타일, 라우팅, 환경변수
  - REST: 경로, 메서드, 공통 응답·오류 형식, 에러 코드 접두사와 번호 규칙, 페이징, 버전
  - 백엔드 ↔ 프론트 연관: 한쪽을 바꾸면 다른 쪽도 같은 커밋에서 바꿔야 하는 것
  - 도구와 검증 명령, AC 테스트 작성법, 주석 예외
- 규칙마다 강도를 붙인다: `[MUST]`, `[SHOULD]`, `NEVER` (1.4)
- 규칙은 기계로 강제할 수 있으면 설정 파일이 정본이다. 이 문서에는 도구, 이유, 예외만 적는다 (5.1)
  → 도구가 못 잡는 규칙(REST 경로, 레이어 사용법, 이름의 의미)은 본문에 적는다
- 키트를 쓰면 키트 골격이 따르는 규칙을 이 문서의 시작점으로 옮긴다 (4.5, 4.7)
- 규칙이 바뀌면 결정 기록(8.1)으로 날짜·이유를 붙인다

### 4.7 백엔드 정본 (백엔드 키트)

- 백엔드 골격은 매번 새로 짜지 않고 백엔드 키트에서 시작한다. 하네스는 키트를 쓰는 방법만 규범으로 정한다
  → 레이어, 공통 응답·예외, 인증 인프라, 설정 같은 기반 코드는 프로젝트마다 거의 같다
- 백엔드 키트 저장소: `harness-psw-backend` (비공개, 태그로 버전 관리)
  - 골격 코드, 선택 헬퍼, 프레임워크별 적용 절차를 둔다
  - 구성 축: 프레임워크 → 골격 / 선택 헬퍼 / 선택 모듈
  - 지원하는 프레임워크와 버전, 라이브러리, 검증 기록은 키트가 소유한다. 키트에 없는 프레임워크는 키트에 먼저 추가한다
- 하네스와 키트의 경계
  - 하네스: 무엇을 지키는가. 골격에 담는 범위, 문서 조각과 승인, 정본 위치(에러 코드, 레이어 구조), 키트 태그를 결정 기록으로 남기는 것
  - 키트: 어떻게 하는가. 패키지 구조와 클래스 이름, 라이브러리, 도구와 명령, 적용 스크립트, 태그
  → 프레임워크를 추가하거나 키트 태그를 올려도 하네스를 고치지 않는다
- 적용 방식: 설계 단계에서 키트 태그와 선택 헬퍼를 골라 결정 기록으로 남기고, 키트의 적용 절차로 파일을 프로젝트에 복사한다
  - 태그는 가장 최근 태그를 권장한다. 키트는 검증을 마친 커밋에만 태그를 단다
  - 복사한 뒤에는 프로젝트 쪽이 정본이다. 키트를 고쳐도 진행 중인 프로젝트는 바뀌지 않는다
  - 라이브러리(jar)로 의존하지 않는다
    → 골격은 프로젝트마다 고쳐 쓰는 코드다. 라이브러리로 묶으면 고칠 때마다 키트를 다시 배포해야 한다
  - 키트의 기본 도구(포맷·lint, CI)를 그대로 쓰면 키트 태그 결정 기록에 함께 적는다. 바꾸면 따로 적는다
  - 적용 결과는 `architecture.md` 백엔드 절에 적는다: 키트 태그·헬퍼, 코드 루트, DB 마이그레이션 경로, 테스트 위치
    → 구현자·검증자가 마이그레이션 경로와 테스트 위치를 이 절에서 읽는다
  - 키트 골격의 규칙은 `conventions.md`의 백엔드·DB·REST 절로 옮긴다 (4.6)
  - 골격 테스트가 사용자 도메인을 대신하는 부분(테스트용 인증 주체 조회 등)은, 사용자 도메인을 구현하는 FR에서 검증자가 실제 도메인에 맞게 고친다. 무엇을 고치는지는 키트 적용 절차에 적는다
- 골격에 담는 것
  - 레이어 구조와 패키지 골격
  - 공통 응답 형식, 에러 코드, 전역 예외 처리
  - 공통 엔터티(감사 필드, 소프트 삭제), 쿼리 설정
  - 인증 인프라: 토큰 발급·검증·재발급·폐기와 인증 필터
    → 로그인 방식과 사용자 계정은 도메인이라 담지 않는다. 인프라는 인증 주체(사용자 ID, 역할)만 안다
  - 설정 골격: 환경별 프로필, 환경변수를 한 곳에서 읽고 시작할 때 검증한다 (5.3)
  - 레이어 규칙 검사 테스트, AC 테스트 기반(5.2)
  - 기본 도구 설정: 포맷·lint, CI
- 골격에 담지 않는 것
  - 도메인 코드 (사용자 계정, 로그인, 역할, 메뉴, 파일 등)
    → 모든 기능 코드는 근거 요구사항이 있어야 한다 (1.2). 근거 없는 코드는 추적이 끊긴다
  - 배포 스크립트, 로그 수집 스택
- 문서 조각
  - 테이블이나 API를 만드는 코드는 골격·헬퍼·모듈 어디에 있든 REQ 초안을 함께 담는다
    → 인프라 코드라도 테이블과 API에는 근거 요구사항이 있어야 한다
  - 적용할 때 REQ 초안은 설계 단계에서 REQ 파일로 만들지 않는다. 기획 루프(7.2)로 넘겨 범위를 확인하고 REQ로 만든다
    → 요구사항은 기획 단계가 소유한다. 키트가 준 초안이라도 범위 확인 없이 들어오면 추적이 끊긴다
- 선택 모듈 (도메인)
  - 프로젝트에서 검증된 도메인만 모듈로 추출한다. 키트에서 먼저 설계하지 않는다
    → 검증 전에 만든 모듈은 첫 적용에서 틀이 바뀌어 고칠 곳만 늘어난다
- 공통 엔터티
  - 삭제는 소프트 삭제가 기본이다. 하드 삭제는 `conventions.md` DB 절의 하드 삭제 예외 표에 이유를 적은 테이블만 한다
    → 기본을 소프트로 두어야 이유 없는 물리 삭제가 생기지 않는다. 예외를 표시하고 검사하는 방법은 키트가 정한다
  - 감사 필드의 행위자는 비지 않는다. 로그인 전 요청과 시스템 작업은 예약된 시스템 계정으로 남긴다
    → 행위자가 빈 저장을 허용하면 인증 누락 버그가 조용히 묻힌다
    - 시스템 계정 행은 사용자 도메인이 만든다. 키트는 그 ID를 설정으로 받는다
  - 행위자 개념이 없는 테이블(로그, 토큰 등)은 시각 필드만 둔다
- 레이어 규칙
  - 계층과 의존 방향은 키트의 프레임워크 절차가 소유한다. 프로젝트는 `architecture.md` 모듈 경계 절에 적용한 구조를 적는다
  - 테스트로 강제한다 (5.1). 규칙의 이유와 예외는 `conventions.md`에 둔다
- 에러 코드
  - 목록의 정본은 백엔드 코드의 에러 코드 정의다. 형식(오류 응답 구조, 접두사, 번호 규칙)은 `conventions.md` REST 절이 정한다
    → 도메인 API 문서를 두지 않으므로 목록을 문서에 따로 두면 코드와 어긋난다 (4.2)
  - `conventions.md` 템플릿의 REST 절은 키트 골격의 오류 응답 형식과 같게 둔다
    → 적용 직후 문서와 코드가 어긋나지 않는다
  - 어느 한쪽을 바꾸면 템플릿과 키트를 함께 바꾼다. 키트 README에 맞춘 하네스 커밋을 적는다

## 5. 구현

### 5.1 규칙과 강제 수단

- 구현 규칙: lint, git 규칙, 코드 규칙, 주석 규칙
- 규칙은 기계로 강제한다
  - lint와 포맷: 설정 파일
  - git 규칙: hook 또는 CI
- 코드 규칙 문서는 `design/conventions.md` 한 파일이다 (4.6)
  - 도구로 강제하는 규칙은 "왜"와 예외만 남긴다
    → 문서에 규칙을 다시 적으면 설정 파일과 어긋난다
  - 도구가 못 잡는 규칙은 본문으로 적고 검토자가 확인한다
- MUST 항목의 검증 수단 연결은 1.4를 따른다
- 기술 스택별 도구는 스택의 표준 도구를 쓴다 (예: ESLint + Prettier, Ruff)

#### lint·포맷

- lint는 error만 차단한다. warning은 막지 않는다
- 규칙을 끄는 주석을 쓸 때는 같은 줄에 사유를 적는다
- 포맷은 커밋 전에 자동으로 적용한다

#### git

| 브랜치 | 용도 | 누가 바꾸나 |
|---|---|---|
| `main` | 배포 | 사용자. 배포할 때 `dev`를 합친다. 하네스는 관여하지 않는다 |
| `dev` | 개발 기준 | 문서(기획·설계) 커밋은 `dev`에 바로 한다. 기능 브랜치를 합친다 |
| `feat/<FR-ID>-<요약>` | FR 하나의 구현 (예: `feat/FR-ORD-010-guest-checkout`) | `dev`에서 만들고, 사용자 확인 후 `dev`로 합친다 (9.2) |

| 규칙 | 내용 |
|---|---|
| 브랜치 단위 | 기능 브랜치 1개 = FR 1개 |
| 병합 방식 | 개별 커밋을 유지한다. squash하지 않는다 |
| 금지 | 에이전트는 `main`에 커밋하거나 합치지 않는다. `main`·`dev`에 force push하지 않는다 |

- squash하지 않는 이유: 커밋별 트레일러(5.4)가 뭉개져 추적이 끊긴다
- 작업 폴더를 따로 두는 방식(git worktree)은 하네스 규칙이 아니다. 필요할 때 써도 된다. `guard-paths.sh`는 worktree 안의 파일에도 같은 규칙을 적용한다

#### 코드 이름과 구조

- 코드 이름은 용어집(`glossary.md`)의 영문명을 따른다
  → 같은 개념이 여러 이름으로 갈라지는 것을 막는다
- 디렉터리 구조와 의존 방향은 `architecture.md`의 모듈 경계와 `conventions.md`를 따른다. 가능하면 도구로 검사한다

#### 주석

- 공개 함수와 모듈에는 문서 주석을 필수로 단다 (lint로 강제)
- 내부 코드에는 필요할 때만 "왜"를 적는다. "무엇"은 코드가 설명한다
- 요구사항 ID는 적지 않는다 (1.2)
- `TODO`에는 참조를 붙인다 (예: `TODO(OPEN-021)`). 참조 없는 TODO는 쓰지 않는다
- 주석 처리한 코드는 남기지 않는다
- 임시 우회에는 사유와 해제 조건을 함께 적는다
- 주석은 한국어로 쓴다

```ts
/**
 * 비회원 주문을 생성한다.
 * 휴대폰 인증이 끝난 요청만 받는다.
 * @throws VerificationRequiredError 인증 토큰이 없거나 만료된 경우
 */
export function createGuestOrder(input: GuestOrderInput): Order { ... }

// PG사가 동일 주문번호 재요청을 중복 결제로 처리하므로, 재시도할 때는 새 멱등 키를 쓴다
const idempotencyKey = crypto.randomUUID();

// TODO(OPEN-012): 동시 로그인 허용 기기 수가 정해지면 제한 로직 추가

// eslint-disable-next-line @typescript-eslint/no-explicit-any -- PG SDK가 타입 정의를 제공하지 않음
const response: any = await pgClient.approve(payload);

// 택배사 API가 주말에 배송 완료 이벤트를 늦게 보내므로, 72시간 뒤 자동으로 완료 처리한다.
// 택배사 웹훅이 실시간으로 바뀌면 제거한다.
```

| 금지 예 | 이유 |
|---|---|
| `// 주문 상태를 PAID로 바꾼다` | 코드가 이미 설명하는 "무엇"을 되풀이한다 |
| `// TODO: 동시 로그인 제한 확인` | 참조가 없어 추적할 수 없다 |
| `// FR-ORD-010 비회원 주문 처리` | 요구사항 ID는 커밋 트레일러에만 적는다 |
| `// const discount = calcLegacyDiscount(order);` | 이전 코드는 git에 있다 |

### 5.2 완료 정의(DoD)

- REQ 수용 기준 충족 (판정 규칙은 1.4)
- AC 테스트 통과
- 직접 검증 AC 통과 (검증자 보고)
- lint 통과
- 주석 작성 및 규칙 통과
- 요구사항 ID 커밋 태깅 (5.4)

#### 검증 방식

- AC 성격에 따라 검증 방법을 나눈다

| AC 성격 | 검증 방법 | 코드 산출물 |
|---|---|---|
| API·데이터로 확인 가능 | AC 테스트 코드 (API 통합 테스트) | 있음 |
| 화면으로만 확인 가능 (문구, 배치, 표시 여부) | 에이전트 직접 검증 | 없음 |
| 상태 전이 표의 각 행 (3.4) | AC 테스트 코드 | 있음 |

- AC 테스트는 사용자가 보는 동작을 API나 화면 수준에서 확인한다
  → 내부 함수 단위로 쪼개면 AC와 1:1로 대응되지 않는다
- 내부 함수 단위 테스트는 만들지 않는다
- `[MUST]` AC는 테스트나 직접 검증이 필수다. `[SHOULD]` AC의 테스트 코드는 선택이다
- AC 테스트는 구현 코드보다 먼저 작성한다. 입력은 FR의 AC와 구현자가 먼저 쓴 계약 코드다 (9.2)
  → 구현에 맞춰 테스트를 쓰는 것을 막는다
- 테스트 이름에 AC ID를 단다 (예: `FR-ORD-010 AC-1: 비회원 주문 생성`)
- MUST: 테스트를 통과시키려고 테스트를 고치지 않는다
  → 요구사항이 바뀐 것이면 승인된 내용의 변경 절차(7.3)를 거친다
- AC 결과는 검증자가 보고하고, 오케스트레이터가 병합 확인 요약(9.2)에 담아 사용자에게 보인다
  - 항목: AC별 결과(PASS/FAIL), 방법(테스트 이름 또는 직접 확인한 화면), 미충족 `[SHOULD]` 사유
  - 파일이나 커밋으로 따로 남기지 않는다
    → 테스트로 확인하는 AC는 테스트 코드가 남는다. 직접 확인하는 AC는 기능이 바뀔 때 다시 확인한다
- 검증 중 만든 임시 스크립트는 커밋하지 않는다
  → 저장소에 남기면 사실상 테스트 코드가 된다
- 코드가 바뀌면 ID 추적(1.2)으로 영향받는 기능을 찾아, 직접 검증 AC를 다시 검증한다
  → AC 테스트 코드는 CI가 자동으로 다시 확인한다
- AC 테스트는 검증자가 구현 전에 작성한다 (9.1)

### 5.3 환경변수·시크릿 관리

- **에이전트 규칙**
  - **NEVER: 실제 시크릿 값을 읽거나 출력하지 않는다**
  - **NEVER: 시크릿 값을 로그, 문서, 커밋, 테스트 데이터에 넣지 않는다**
  - **MUST: 시크릿이 필요하면 사용자에게 설정을 요청한다**
- 커밋 금지
  - NEVER: 시크릿을 저장소에 커밋하지 않는다
  - 커밋 전 스캔과 CI 스캔으로 강제한다 (예: gitleaks)
- `.env`
  - git에서 제외한다
  - `.env.example`에 키 이름만 적어 커밋한다
    → 필요한 환경변수 목록의 정본이다
- 환경 구분: local / dev / prod
  - 배포 환경의 값은 시크릿 저장소에 둔다. 도구 (프로젝트별 결정, 1.5)
- 코드 접근
  - 환경변수는 설정 모듈 한 곳에서만 읽는다
  - 앱 시작 시 검증하고, 누락되면 바로 실패한다
- 유출 시 즉시 폐기하고 재발급한다
  → git 이력을 지우는 것으로는 해결되지 않는다
- 외부 연동 키의 준비 상태는 `architecture.md` 외부 연동 절의 테스트 환경 항목과 연결한다

### 5.4 커밋 메시지 규칙

```text
feat: 비회원 주문 추가

비회원도 휴대폰 인증으로 주문을 조회할 수 있게 한다.

Refs: FR-ORD-010, FR-ORD-004
Closes: OPEN-021
```

- 형식: `type: 제목` + 본문(선택) + 트레일러. scope는 쓰지 않는다
- type: `feat`, `fix`, `refactor`, `test`, `docs`, `chore`, `build`, `ci`
- 제목은 한국어로, 무엇을 했는지 적는다
- 길이 (문자 개수 기준)

| 대상 | 권장 | 차단 |
|---|---|---|
| 제목 (type 포함) | 50자 이내 | 72자 초과 |
| 본문 한 줄 | - | 없음 (100자 초과 시 경고만) |
| 본문 전체 | "무엇을, 왜"를 몇 줄로 | 없음 |
| 트레일러 줄 | - | 없음 |

  → 긴 설명이 필요하면 본문이나 PR 본문에 적는다
- 트레일러

| 키 | 값 | 의미 |
|---|---|---|
| `Refs:` | 요구사항 ID (`FR-`, `NFR-` 등) | 이 커밋이 다루는 요구사항 |
| `Closes:` | `OPEN-NNN` | 해결한 미결 |

- ID는 제목에 넣지 않고 트레일러에만 적는다
  → 제목이 읽기 쉽고, `git interpret-trailers`로 기계가 뽑아낼 수 있다
- 결정의 경위(왜 바꿨나)는 결정 기록(8.1)과 커밋 본문에 적는다
- 트레일러 필수 조건

| 변경 대상 | 필수 트레일러 |
|---|---|
| 코드 + `feat` / `fix` | `Refs:` |
| 코드 + 그 외 type | 없음 |
| REQ 최초 작성 | `Refs:`에 새로 만든 도메인·영역 접두사 (예: `Refs: FR-ORD, NFR-PERF`) |
| `docs/req/`, `docs/design/` | `Refs:` 또는 `Closes:` 중 하나 이상 |
| `docs/open-question.md`, `docs/glossary.md`만 | 없음 |

- `approved` 문서의 내용을 바꾸면 같은 커밋에서 `status`를 `draft`로 되돌린다 (1.1). 검사 스크립트가 막는다
- 강제 수단 (기본안: 하네스 스크립트 `check-commit-msg.sh`를 `.githooks/commit-msg`에 연결하고 `core.hooksPath`로 켠다)
  - commit-msg hook: 형식, 길이, 필수 트레일러, ID 존재 여부를 검사한다
  - CI: 같은 검사를 다시 실행한다
    → hook은 `--no-verify`로 건너뛸 수 있다
  - 도구 (프로젝트별 결정, 1.5). 검사 스크립트는 하네스가 제공한다

## 6. 운영·유지보수

- 대상: 배포, 모니터링, 장애 대응
- 최소 구조에서는 제외한다. 필요해지면 이 절과 문서 위치를 먼저 정한다

## 7. 피드백 루프

- 인터뷰 누락이나 잘못된 방향을 발견하면 인터뷰를 다시 진행하고, 영향받은 REQ를 다시 산출한다
- 기획과 설계가 끝날 때까지 각 단계에서 반복 호출한다

### 7.1 4요소

| 요소 | 기획 루프 | 설계 루프 |
|---|---|---|
| 트리거 | 누락, 모순, 실현 불가능, 사용자 번복 | 교차 검증 불일치(4.3), 설계 문서 간 모순, 기술적 실현 불가능, 구현 중 설계 결함 발견, 사용자 번복 |
| 개정 방식 | 최신만 정본 + git. 파일별 `approved`로 승인 | 최신만 정본 + git. 파일별 `approved`로 승인 |
| 영향 분석 | ID로 영향받는 REQ만 다시 산출 | 바뀐 설계 요소를 참조하는 문서만 다시 검토 |
| 종료 조건 | 미정 OPEN 0건 | 도메인 단위로 교차 검증 통과, 미정 OPEN 0건, 해당 설계 문서 모두 `approved` |

- 개정 방식 공통: 최신 상태만 정본으로 두고, 이력은 git에 둔다. 결정 경위는 결정 기록(8.1)으로 남긴다
  - 탈락안
    - 파일 내 이력 절 누적 → 파일이 계속 커진다
    - 버전별 전체 사본(`v1.0/`, `v1.1/`) → 옛 버전을 잘못 읽을 위험이 있고, 검색 결과가 오염된다
    - 원본 + 증분 부록 → 에이전트가 전부 읽고 병합해야 해서 토큰 소모와 모순 위험이 가장 크다
  - 태그를 찍지 않는다
    → 도메인마다 끝나는 시점이 다르고, 진입 조건(1.1)이 파일별 `approved`를 본다
- 종료 조건 공통
  - 남은 미결을 모두 "보류"로 돌린 경우도 종료로 본다
    → 종료 조건이 없으면 루프가 끝나지 않는다
  - `loop-status.sh`로 확인한다: 미정 OPEN과 끊긴 자리표시가 0건이어야 한다 (8.2)
- 설계 루프는 도메인 단위로 종료한다
  → 한 도메인의 설계가 끝나면 다른 도메인을 기다리지 않고 구현을 시작할 수 있다
- 설계 루프의 영향 분석 경로

| 바뀐 것 | 다시 볼 곳 |
|---|---|
| 상태 전이 (`_policy.md`) | 해당 상태를 바꾸는 FR, 관련 AC 테스트, 해당 목업 |
| 화면 (IA) | 해당 목업, 셸 메뉴 |
| 컴포넌트, 테마 | 해당 컴포넌트를 쓰는 목업 (`data-component`로 찾는다) |
| 코드 규칙 (`conventions.md`) | 규칙을 따르는 기존 코드. 고칠지 사용자에게 확인한다 |

- 교차 검증의 스크립트 항목은 매번 전체를 실행하고, 검토자 항목은 영향받은 문서만 다시 본다
- 반복 한도: 같은 불일치가 2회 이상 다시 나오면 요구사항 문제로 보고 사용자에게 보고한다
  → 설계 루프가 헛돌지 않게 한다

### 7.2 단계 사이 루프 규칙

- 설계 중 기획 결함을 발견하면 설계 단계에서 요구사항을 고치지 않는다
- 기획 루프를 호출해 REQ에 반영한 뒤 설계로 돌아간다
  → 하위 문서에서 상위 결정을 변경하지 않는다 (1.1)
- 구현 중 설계 결함을 발견하면 구현 단계에서 설계를 고치지 않는다
- 설계 루프를 호출해 설계 문서에 반영한 뒤 구현으로 돌아간다

```text
기획 루프 ◀── 요구 결함 ── 설계 루프 ◀── 설계 결함 ── 구현
```

### 7.3 승인된 내용의 변경

- 구분
  - `draft` 문서 수정: 그대로 고친다. 결정이 끼면 결정 기록(8.1)을 붙인다
  - 누락·모호함 발견: `open-question.md`에 등록한다 (8.2)
  - 승인된 내용을 바꾸거나 없애야 함 (트리거: 모순, 실현 불가능, 사용자 번복): 아래 절차
  - 미결의 답으로 승인된 문서에 내용을 추가함: 아래 절차 없이 반영하고 `approved → draft` 규칙(1.1)으로 재승인받는다
- 절차
  1. 작업 중이던 단계를 멈춘다
  2. 사용자에게 변경안을 보인다: 대상 ID, 바꿀 내용, 사유, 영향 범위(`find-refs.sh`)
  3. 사용자가 수락하면 소유 단계의 절차로 반영하고, 반영한 `approved` 문서는 `draft`로 되돌린다. 바뀐 값에는 결정 기록(8.1)을 붙인다
  4. 사용자가 거절하면 반영하지 않는다. 같은 요청이 다시 나올 만하면 해당 문서에 거절한 이유를 결정 기록으로 남긴다
- MUST: 수락과 거절은 사용자만 정한다
- 변경안을 파일로 따로 남기지 않는다
  → 수락되면 결과가 원천 문서와 git에 남고, 거절되면 원천이 바뀌지 않는다

## 8. 결정과 미결

- 결정과 미결은 별도 기록 폴더를 두지 않는다
  - 결정: 그 값이 적힌 원천 문서에 날짜·이유와 함께 적는다 (8.1)
  - 미결: `docs/open-question.md` 한 파일에 모은다 (8.2)
  → 구현 에이전트가 원천만 읽어도 값과 이유를 함께 본다. 결정 파일을 따로 두면 원천과 결정 파일을 오가야 하고, 대체된 결정을 잘못 따를 위험이 생긴다
- 이력(언제 무엇에서 무엇으로 바뀌었나)은 git에 둔다

### 8.1 결정 기록

- 결정은 결정된 값 바로 옆에 날짜와 이유를 적는다
  - 형식: `<값> 〔YYYY-MM-DD〕 <이유>`
  - 예: `유휴 세션 만료: 60분 〔2026-09-25〕 공용 PC 사용이 많아 짧게 둔다`
  - 표에서는 근거 열에 `〔YYYY-MM-DD〕 <이유>`를 적는다
- 이유는 한두 줄로 쓴다. 대안 비교처럼 길어지면 커밋 본문에 적고, 문서에는 결론과 한 줄 이유만 남긴다
- 결정이 바뀌면 값과 날짜·이유를 새로 쓴다. 이전 결정은 지우고 git에 맡긴다
  → 옛 결정이 문서에 남아 있으면 에이전트가 무효인 결정을 따를 수 있다
- MUST: 결정은 사용자가 한다. 에이전트는 선택지와 추천안만 제시한다
- 인터뷰 답을 그대로 옮긴 값에는 결정 기록을 붙이지 않아도 된다. 선택지 중에서 고른 값, 추천안과 다르게 정한 값, 나중에 번복될 만한 값에는 붙인다

### 8.2 미결사항 (`open-question.md`)

- 아직 정하지 않은 것(미정)과 정하지 않고 진행하기로 한 것(보류)을 모은다
- 단계를 가리지 않는다. 기획·설계·구현 중 생긴 미결을 모두 이 파일에 둔다
- 수명주기: 등록 → 해결(원천 반영 후 항목 삭제) 또는 보류
- 미결은 원천에 자리표시만 두고 내용은 이 파일에 둔다
  → 예: `동시 로그인: [OPEN-012]`
- 해결되면 자리표시를 값과 결정 기록(8.1)으로 바꾸고, 이 파일에서 항목을 지운다
  → 파일에 남은 항목은 모두 아직 답이 없는 질문이다 (보류 포함)
  → 질문 원문은 git 이력에 남는다
- OPEN ID는 재사용하지 않는다
- 항목 형식 (스크립트가 읽는다)

```markdown
## OPEN-012 동시 로그인 허용 기기 수

- 상태: 미정
- 단계: 기획
- 발견: 2026-09-25
- 위치: docs/req/functional/auth/_policy.md
- 체크리스트: ck-sec-3
- 질문: 한 계정으로 동시에 로그인할 수 있는 기기는 몇 대인가
- 선택지: 1대(보안 우선) / 3대(편의 우선, 추천)
- 보류 사유:
```

- 상태는 `미정`과 `보류` 둘만 쓴다
- 체크리스트, 선택지, 보류 사유는 해당할 때만 채운다
- 보류로 돌리면 상태를 `보류`로 바꾸고 날짜와 사유를 적는다. 자리표시는 남긴다
  - MUST: 보류는 사용자만 정한다
  - 보류 항목은 피드백 루프 종료와 `approved` 전환을 막지 않는다 (1.1, 7.1)
- 원천의 자리표시에 대응하는 항목이 없으면 끊긴 자리표시로 보고 오류로 처리한다

### 8.3 용어집 위치

- 용어집은 `docs/glossary.md`에 둔다
  → 경위가 아니라 코드 네이밍까지 좌우하는 정본이고, 모든 단계가 함께 쓴다

## 9. 확장: 하위 에이전트

- 역할별 에이전트를 두고, 작업 대상 FR 경로를 컨텍스트로 넘긴다

### 9.1 역할과 모델

| 역할 | 하는 일 | 권한 | 하지 않는 일 | 모델 |
|---|---|---|---|---|
| 오케스트레이터 (메인 세션) | 작업 분배, 반복 횟수 관리, 에스컬레이션, 사용자 보고, `dev`로 병합 | 전체 | 직접 구현 | 메인 세션 모델 |
| 구현자 | FR 1개의 계약과 구현 | 편집 (기능 브랜치) | REQ·설계·테스트 파일 수정, 범위 밖 파일 수정 | `opus` |
| 검토자 | 설계 검토(4.3의 검토자 항목), 코드 검토(규칙·요구 정합, 범위 이탈, 보안) | 읽기 전용 | 직접 수정 | `opus` |
| 검증자 | 구현 전 AC 테스트 작성, 구현 후 테스트 실행·화면 AC 직접 검증·AC 결과 보고 | 테스트 파일만 편집 | 구현 코드 수정, 통과시키려고 테스트 수정 | `opus` |

- 검토자는 설계용·코드용 체크리스트를 함께 가진 하나의 에이전트다
  → 둘 다 읽기만 하고 지적한다는 성격이 같다
- AC 테스트는 검증자가 구현 전에 작성하고, 구현자는 통과시키기만 한다
  → 테스트를 쓰는 쪽과 통과시키는 쪽을 나눠야 검증이 독립적이다
- 모델은 버전 없이 별칭으로 지정한다
  → 새 모델이 나와도 에이전트 파일을 고치지 않는다
- 처음 운영하는 하네스이므로 세 역할 모두 `opus`로 시작한다
  → 운영해 보고 비용 대비 품질을 확인한 뒤 역할별 조정을 검토한다
- FR은 한 번에 하나씩 진행한다
  → 동시에 진행하면 작업 폴더 분리, 공유 파일 반영 순서, 마이그레이션 번호 충돌 같은 규칙이 더 필요하다. 지금 규모에서는 순서대로가 싸다
- 에이전트 파일은 오케스트레이터를 뺀 3개(`implementer.md`, `reviewer.md`, `verifier.md`)다
  - 하네스 저장소 `agents/`에 두고, `install.sh`가 `.claude/agents/`로 복사한다

### 9.2 흐름

```text
오케스트레이터
  → 구현자: 계약 작성 (API 경로·요청·응답 형식, 오류 코드, DB 마이그레이션. 동작은 비워 둔다)
  → 검증자: FR의 AC와 계약으로 테스트 작성 (구현 전)
  → 구현자: 구현
       └ 설계·요구 결함 발견 → 즉시 중단하고 보고 → 해당 루프 호출 (7.2)
  → lint·타입 검사 (도구 검사)
  → 검토자: 코드 검토 ── 수정 필요 → 구현자로
  → 검증자: 테스트 실행 + 화면 AC 직접 검증 ── FAIL → 구현자로
  → 사용자 확인 (FR마다) → dev로 병합
```

- 모든 작업은 `dev`에서 만든 기능 브랜치 `feat/<FR-ID>-<요약>`에서 한다
- 검토자는 구현과 lint가 끝난 뒤, 검증자의 판정 전에 본다
  → 완성된 코드를 읽어야 판단할 수 있고, 도구가 잡는 것에 검토자를 쓰지 않는다. 검토로 코드가 바뀌면 판정을 다시 해야 하므로 판정은 코드가 확정된 뒤 한 번 한다

- 계약 단계는 서버 쪽 변경이 없는 FR(화면만 바뀌는 기능)이면 건너뛴다
  → 계약은 `conventions.md` REST·DB 절을 따른다. 도메인 API 문서가 없으므로 검증자는 이 코드로 경로와 형식을 안다 (4.2)
- 병합 전 검사: 브랜치의 커밋마다 `check-commit-msg.sh --commit`
- 병합 전 사용자 확인은 FR마다 받는다
- 확인 요청은 아래 요약 형식으로 한다

```text
[병합 확인] FR-ORD-010 비회원 주문
- 변경 파일: 5개 (코드 3, 테스트 2)
- AC 결과: AC-1 PASS(테스트) / AC-2 PASS(직접 검증) / AC-3 FAIL [SHOULD] 사유: SMS 키 미준비
- 검토 지적: 2건 → 모두 수정됨
- 남은 미결: 없음
→ 병합할까요?
```

### 9.3 원칙

| 원칙 | 내용 |
|---|---|
| 컨텍스트 독립 | 검토자와 검증자에게는 구현자의 설명을 주지 않고, 산출물과 원천 문서만 준다 |
| 지적과 수정 분리 | 지적하는 쪽은 고치지 않는다 |
| 근거 ID 필수 | 모든 지적에 근거 ID를 단다. 근거가 없으면 "제안"으로 분류한다 |
| 반복 한도 | 같은 AC가 3회 FAIL하면 설계·요구 문제로 보고 사용자에게 보고한다 |
| 디자인 파일 | 구현자는 테마 파일과 UI 컴포넌트 코드를 고치지 않는다. 바꿀 것이 있으면 설계 루프로 보낸다. 경로는 `architecture.md` UI 절에 있다 |
| 마이그레이션 | 구현자는 DB 마이그레이션을 새 파일로만 추가한다. 이미 있는 파일은 고치지 않는다. 경로는 `architecture.md` 백엔드 절에 있다 |
| 항상 분리 | 작은 작업이라도 검토와 검증은 별도 에이전트가 한다 |

- 컨텍스트 독립: 구현자의 논리에 설득되면 결함을 놓친다
- 지적과 수정 분리: 스스로 고치면 기준이 낮아진다
- 반복 한도가 설계 루프(2회, 7.1)보다 1회 많은 이유: 구현은 시행착오가 더 많다
- 디자인 파일을 막는 이유: 커스터마이즈는 테마 변수로만 한다 (4.5). FR 작업 중에 바꾸면 다른 화면의 모양이 알리지 않고 바뀐다
- 마이그레이션을 새 파일로만 추가하는 이유: 이미 적용된 파일을 고치면 다른 환경의 DB와 어긋난다

### 9.4 권한 강제

- 편집 시점(hook)과 도구 제한으로 막는다
- 도구 제한: 에이전트 파일의 `tools` 필드로 막는다. 검토자에게는 읽기 도구만 준다
  → 검토자에게 Bash를 주면 파일을 고칠 수 있으므로, diff는 오케스트레이터가 뽑아서 넘긴다
  - 검증자는 화면 검증에 브라우저 도구가 필요하므로 `tools`를 제한하지 않고 hook으로 경로만 막는다
- 편집 시점: 프로젝트 `.claude/settings.json`의 PreToolUse hook이 `guard-paths.sh hook`을 실행한다
  - hook 입력의 `agent_type`(하위 에이전트 이름)으로 역할을 알아낸다
  - 경로는 파일이 속한 git 작업 폴더 기준으로 판단한다 (worktree를 쓰더라도 같은 규칙)
  - Bash로 하는 편집은 hook이 판단하지 못한다. 검토자가 diff에서 역할 밖 파일 변경을 확인한다

| 역할 | 편집할 수 있는 경로 |
|---|---|
| `implementer` | 테스트 파일, `docs/`, `.claude/`, `CLAUDE.md`를 뺀 나머지 |
| `verifier` | 테스트 파일 |
| `reviewer` | 없음 |

- 테스트 파일 판단: `.claude/psw.conf`의 `PSW_TEST_GLOBS`

## 10. 하네스 저장소 구성

### 10.1 저장소 구조

```text
harness-psw-standards/
  README.md                 저장소 소개, 설치 방법
  harness-psw.md            설계서 (정본)
  install.sh                프로젝트에 스킬·에이전트·스크립트를 복사
  skills/
    <skill>/
      SKILL.md
      references/           그 스킬이 만드는 문서의 템플릿, 체크리스트
      templates/            그 스킬이 프로젝트에 복사하는 파일
  agents/                   implementer.md, reviewer.md, verifier.md
  scripts/                  검사 스크립트
```

- 템플릿은 그 파일을 만드는 스킬이 소유한다
  - 문서 템플릿, 체크리스트: `references/`
  - 프로젝트에 그대로 복사하는 파일: `templates/` (예: `psw-init`의 프로젝트 골격, `psw-design`의 설정 템플릿)
  → 한곳에 모아두면 스킬과 템플릿이 따로 논다
  → 설치 후에는 프로젝트에 복사된 스킬만 접근할 수 있으므로, 템플릿이 스킬 밖에 있으면 찾을 수 없다
- 여러 스킬이 함께 쓰는 것(스크립트, 에이전트)만 최상위에 둔다
- 관련 저장소: `harness-psw-frontend` (프론트 키트, 비공개, 4.5)
  → 테마를 계속 늘리고 프레임워크를 추가할 계획이라 하네스와 버전을 따로 관리한다
- 관련 저장소: `harness-psw-backend` (백엔드 키트, 비공개, 4.7)
  → 프로젝트에서 검증된 모듈을 계속 추출할 계획이라 하네스와 버전을 따로 관리한다

### 10.2 스킬 목록

- 기존 스킬과 이름이 겹치지 않도록 `psw-` 접두사를 쓴다

| 단계 | 스킬 | 하는 일 | 설계서 |
|---|---|---|---|
| 준비 | `psw-init` | 프로젝트 골격 생성 (`CLAUDE.md`, `docs/`, `open-question.md`, `glossary.md`, `.env.example`, `.gitignore`, `.gitattributes`), 승인 보호 hook | 0.1, 1.1, 2절 |
| 기획 | `psw-interview` | 주제·레퍼런스 제시, 인터뷰 진행, 답을 REQ 초안에 기록, 미결 등록 | 3.1, 부록 A |
| 기획 | `psw-req` | REQ 상세 작성 (FR, `_policy`, 영역 기준, 권한 매트릭스, 인덱스), 누락 검사, 승인 요청 | 3.2~3.5 |
| 설계 | `psw-design` | 프로젝트별 결정 항목 채우기, 설계 문서·코드 규칙·UI 기초·목업 작성 | 1.5, 4절 |
| 설계 | `psw-crosscheck` | 교차 검증 실행 (스크립트 + 검토자) | 4.3 |
| 구현 | `psw-implement` | FR 단위 흐름 진행 (구현자 계약 → 검증자 → 구현자 → 검토자 → 검증자 → 병합 확인 → dev 병합) | 5절, 9절 |
| 공통 | `psw-change` | 피드백 루프: 미결 등록·해결·보류, 승인된 내용의 변경, 영향 분석, 종료 조건 확인 | 7절, 8절 |

- `psw-design`은 문서별 템플릿을 `references/`로 나눠 하나의 스킬로 둔다. 커지면 UI 부분을 `psw-ui`로 분리한다
- `psw-change`는 기획·설계·구현 모두에서 호출된다
  → 각 스킬에 같은 절차를 복사하지 않는다
- 진행 현황과 RTM은 스크립트로 만들고, 스킬로 두지 않는다

### 10.3 설치

- 전역에 설치하지 않는다. `install.sh <프로젝트 경로>`로 프로젝트에 복사한다
  → 전역에 설치하면 모든 프로젝트에 걸려 다른 방법론과 섞인다
  → `psw-init`도 프로젝트에 먼저 있어야 실행할 수 있으므로, 첫 설치는 스크립트로 한다
- 복사 위치
  - 스킬: `.claude/skills/psw-*/`
  - 에이전트: `.claude/agents/`
  - 검사 스크립트: `.claude/scripts/psw/`
- 다시 설치하면 하네스가 소유한 파일(`psw-*` 스킬, 하네스 에이전트 3개, `scripts/psw/`)만 교체하고, 프로젝트의 다른 파일은 건드리지 않는다
  - 하네스에서 없어진 `psw-*` 스킬은 다시 설치할 때 지운다
- 설치한 하네스 버전(커밋 해시)을 `.claude/psw-version`에 남긴다
- hook·CI 설정은 설치 시 넣지 않는다
  - 승인 보호·역할 경로 hook: `psw-init`이 사용자 동의를 받아 `.claude/settings.json`에 넣는다
  - 커밋 검사 hook·CI: 커밋 검사 도구가 프로젝트별 결정 항목(1.5)이므로 `psw-implement` 준비 단계에서 넣는다
    - 백엔드 키트가 CI를 가져왔으면 그 워크플로에 커밋 검사를 더한다 (4.7)

### 10.4 구축 후 확인할 것

- 스킬 절차는 스크립트처럼 자동 시험할 수 없다. 첫 실제 프로젝트에서 단계마다 확인하고 설계서와 스킬을 고친다
- 편집 시점 hook은 워크스페이스 신뢰를 수락해야 동작한다. 첫 실행 때 동작을 확인한다

### 10.5 검사 스크립트

| 스크립트 | 하는 일 | 설계서 |
|---|---|---|
| `next-id.sh` | 다음 ID를 발급한다 (OPEN, 요구사항·화면 ID). 삭제된 ID도 git 이력에서 찾아 재사용하지 않는다 | 1.2 |
| `loop-status.sh` | 루프 종료 조건을 확인한다 (미정 OPEN, 끊긴 자리표시) | 7.1, 8.2 |
| `find-refs.sh` | ID를 참조하는 곳을 찾는다 (영향 분석) | 1.2, 7.1 |
| `checklist-coverage.sh` | 인터뷰 체크리스트의 필수 항목이 REQ 반영·미결·해당 없음 중 하나로 처리됐는지 확인한다 | 3.1 |
| `crosscheck.sh` | 설계 교차 검증의 스크립트 항목(1, 2, 4, 5, 6, 7)을 실행한다 | 4.3 |
| `guard-paths.sh` | 편집 시점 hook: 역할별 경로와 approved 전환을 막는다 | 1.1, 9.4 |
| `approve.sh` | 사용자가 직접 실행해 문서를 approved로 바꾼다 | 1.1 |
| `check-commit-msg.sh` | 커밋 메시지 형식·길이·트레일러·ID 존재·approved 문서 변경을 검사한다 | 5.4, 1.1 |
| `rtm.sh` | 추적 매트릭스를 만든다 | 1.2 |
| `doc-size.sh` | 문서 크기 신호를 보여준다 (토큰 추정) | 1.3 |

- 스크립트는 bash로 쓴다
  → 기술 스택과 상관없이 git이 있는 환경이면 실행된다

## 부록 A. 인터뷰 질문 목록

> `skills/psw-interview/references/checklist.md`로 옮겼다. 그 파일이 정본이다.

## 부록 B. 설계 문서별 필수 항목

> `skills/psw-design/references/`로 옮겼다. 그 파일들이 정본이다.

## 부록 C. CLAUDE.md 템플릿

> `skills/psw-init/templates/CLAUDE.md.tmpl`로 옮겼다. 그 파일이 정본이다.
