# harness-psw

> 신규 프로젝트용 하네스 설계서다. 기존 하네스를 대체하기 위해 정리 중이다.
> `(프로젝트별 결정)`은 프로젝트마다 채우는 항목이다 (1.5).
> `(구축 단계)`는 하네스를 만들면서 정하는 항목이다 (10.4).

## 목차

```text
0. 개요
   0.1 진입 문서 (CLAUDE.md)
1. 공통 원칙
   1.1 정본과 파생
   1.2 ID 추적 체계
   1.3 문서 크기 기준
   1.4 MUST 키워드 사용 기준
   1.5 프로젝트별 결정 항목
2. 전체 구조
3. 기획
   3.1 진행 흐름
   3.2 SRS 구조와 소유 경계
   3.3 요구사항 유형과 경로
   3.4 기능 REQ 크기 기준
   3.5 REQ 템플릿과 수용 기준
4. 설계
   4.1 설계 관점
   4.2 설계 문서의 축
   4.3 설계 순서와 교차 검증
   4.4 화면 시안 형식
   4.5 UI 정본 (토큰·공통 컴포넌트·레이아웃)
   4.6 상태 전이 문서
5. 구현
   5.1 규칙과 강제 수단
   5.2 완료 정의(DoD)
   5.3 환경변수·시크릿 관리
   5.4 커밋 메시지 규칙
6. 운영·유지보수
7. 피드백 루프
   7.1 4요소
   7.2 단계 사이 루프 규칙
   7.3 변경 요청(CR, Change Request)
8. records
   8.1 3층 분리
   8.2 결정 기록
   8.3 미결사항 수명주기와 자리표시
   8.4 용어집 위치
9. 확장: 하위 에이전트
   9.1 역할과 모델
   9.2 흐름
   9.3 원칙
   9.4 권한 강제
10. 하네스 저장소 구성
   10.1 저장소 구조
   10.2 스킬 목록
   10.3 설치
   10.4 구축 단계에서 정할 것
부록 A. 인터뷰 질문 목록 (초안)
부록 B. 설계 문서별 필수 항목 (초안)
부록 C. CLAUDE.md 템플릿 (초안)
```

## 0. 개요

- 에이전트가 문서를 정본 삼아 기획부터 구현까지 진행하기 위한 최소 하네스 구조를 정의한다
- 적용 범위: 신규 프로젝트
- 단계 흐름: 기획 → 설계 → 구현
  → 운영·유지보수는 자리만 확보한다 (6절)
- 기획과 설계는 단계가 끝날 때까지 피드백 루프를 반복 호출하며 검증한다 (7절)
- 결정·미결 같은 경위 기록은 원천 문서와 분리된 records에서 관리한다 (8절)

### 0.1 진입 문서 (`CLAUDE.md`)

- `CLAUDE.md`는 어디를 읽을지 알려주는 가벼운 라우터다. 규칙 본문을 옮겨 적지 않는다
  → 매 대화마다 자동으로 읽히므로, 길면 모든 작업에서 토큰이 낭비된다
- 크기: 약 1k 토큰 이내
- 정본 범위
  - 항상 지켜야 할 핵심 규칙: `CLAUDE.md`
  - 절차 세부: 스킬
  - 프로젝트 내용: `docs/`
- 현재 단계는 적지 않는다
  → 금방 틀린 정보가 된다. 에이전트가 단계 진입 조건(1.1)으로 판단한다
- 구성: 작업별 진입 경로, 로딩 원칙, 핵심 규칙, 명령
- 핵심 규칙에는 위반하면 되돌리기 어렵거나 문서 체계가 무너지는 것만 넣는다
  - MUST: 문서를 `approved`로 바꾸지 않는다. 승인은 사용자만 한다
  - MUST: 하위 단계에서 상위 문서를 고치지 않는다. 해당 단계의 루프를 호출한다
  - MUST: 승인된 내용을 바꿔야 하면 CR을 작성한다
  - MUST: 모르는 것은 추측하지 않고 OPEN으로 등록한다
  - MUST: 테스트를 통과시키려고 테스트를 고치지 않는다
  - NEVER: 시크릿 값을 읽거나 출력하거나 커밋하지 않는다
  - MUST: 커밋에 트레일러(`Refs`, `Decision`, `CR`, `Closes`, `Role`)를 단다 (5.4)
- 하네스 초기화 스킬이 템플릿(부록 C)으로 생성하고, 경로나 명령이 바뀔 때만 수정한다

## 1. 공통 원칙

### 1.1 정본과 파생

- md를 정본으로 둔다. PDF는 승인 시점에 md에서 생성하는 파생물이다
- 최신 상태만 정본으로 두고, 이력은 git에 둔다 (7.1 참조)
- 하위 문서에서 상위 결정을 변경하지 않는다 (7.2 참조)
- 문서 상태값은 2개만 둔다 (frontmatter `status:`)
  - `draft`: 작성 중이거나 수정됨. 에이전트가 바꾼다
  - `approved`: 사용자 승인. 다음 단계의 입력으로 쓸 수 있다
- MUST: `approved`로는 사용자만 바꾼다
- 적용 대상은 REQ와 설계 문서다
  → SRS는 폴더 전체를 git 태그로 승인하므로 파일별 상태를 두지 않는다
- `approved` 문서를 수정하면 `draft`로 되돌리고 다시 승인받는다
- `[OPEN-xxx]`가 남은 문서는 `approved`가 될 수 없다. 해당 미결을 "보류로 수용"한 경우는 예외다
- 도메인 단위 일괄 승인을 허용한다
- 구현 완료, 대체, 범위 제외는 상태값으로 두지 않는다
  - 구현 진행 현황은 커밋 트레일러(`Refs: FR-xxx`), AC 테스트 결과, 검증 기록에서 뽑는다
    → 구현할 때마다 REQ 파일을 고치면 요구사항 이력이 오염된다
  - 대체: 파일을 삭제하고 DEC에 경위를 남긴다
  - 범위 제외: SRS 우선순위 `won't`로 표현한다
- 단계 진입 조건
  - 설계 착수: SRS 기준선 태그가 있고, 해당 도메인 REQ가 `approved`
  - 구현 착수: 해당 REQ와 관련 설계 문서가 `approved`이고, 프로젝트별 결정 항목(1.5)이 모두 채워짐

### 1.2 ID 추적 체계

- 요구사항과 설계 산출물에는 고유 ID를 부여한다
  → 피드백 루프나 변경 시 영향 범위를 ID로 좁힐 수 있다
- REQ는 요구사항 문서 전체를 부르는 이름이고, ID 접두사는 유형별로 나눈다

| 대상 | 형식 | 예 |
|---|---|---|
| 기능 | `FR-<도메인>-NNN` | `FR-ORD-001` |
| 비기능 | `NFR-<영역>-NNN` | `NFR-PERF-001` |
| 보안 | `SEC-<영역>-NNN` | `SEC-AUTH-001` |
| 연동 | `INT-<시스템>-NNN` | `INT-PG-001` |
| 데이터 | `DAT-<영역>-NNN` | `DAT-BACKUP-001` |
| 제약 | `CON-NNN` | `CON-001` |
| 수용 기준 | 요구사항 ID + `AC-n` | `FR-ORD-001 AC-2` |
| 화면 | `SCR-<도메인>-NNN` | `SCR-ORD-001` |
| API | `API-<도메인>-NNN` | `API-ORD-001` |
| 테이블 | 테이블 이름 | `orders` |
| 상태 | 엔터티.상태 | `Order.PAID` |
| 컴포넌트 | 컴포넌트 이름 | `Button` |
| 결정·미결·변경 | `DEC-NNNN`, `[OPEN-NNN]`, `CR-NNN` | `DEC-0007` |

- 화면과 API는 이름·경로가 바뀔 수 있으므로 별도 ID를 둔다. 테이블·상태·컴포넌트는 이름 자체를 ID로 쓴다
- 도메인 코드는 `req/README.md` 한 곳에서만 정의한다
- ID는 재사용하지 않는다. 삭제된 ID도 다시 쓰지 않는다
- 참조는 하위 → 상위 단방향으로만 적는다

```text
SRS ← REQ ← 설계(화면·API·상태) ← 테스트·커밋
```

| 위치 | 적는 것 |
|---|---|
| REQ | 출처 SRS 파일 |
| 설계 문서 | 근거 요구사항 ID (예: `근거: FR-ORD-001, NFR-PERF-001`) |
| 테스트 | 테스트 이름이나 태그에 `FR-ORD-001 AC-2` |
| 검증 기록 | AC별 결과에 `FR-ORD-001 AC-2` |
| 커밋 | 트레일러 `Refs: FR-ORD-001` |

  → 상위 문서에 하위 목록을 적으면 같은 연결이 양쪽에 기록되고, 하위가 바뀔 때마다 상위 이력이 오염된다
  → 역방향은 ID 검색으로 찾는다
- 소스 코드 주석에는 요구사항 ID를 적지 않는다
  → 코드는 자주 바뀌고 주석은 갱신되지 않아 금방 틀린 정보가 된다
- 추적 매트릭스(RTM)는 손으로 관리하지 않고, 위 참조를 스크립트로 모아 필요할 때 생성한다

### 1.3 문서 크기 기준

- 원칙: 문서는 선택적 로딩이 가능한 크기로 유지한다 (전역 규칙 준용)
- 신호: 아래를 넘으면 분리를 "검토"한다. 강제 분리하지 않는다
  - 파일당 약 2k 토큰 (한국어 문서는 줄 수보다 토큰으로 판단)
  - 작업 1개당 읽는 파일 3~5개
    → 파일 단위가 아니라 인덱스·참조 구조를 점검하는 기준이다
- 유형별 수치는 각 템플릿이 소유한다
  - REQ 기능 파일, `_policy`, DEC, CR: 신호 기준을 그대로 적용한다
  - ERD, 아키텍처: 응집성을 우선하고, 넘으면 도메인 단위 분할을 검토한다
  - HTML 목업, 자동 생성 파일: 적용하지 않는다
- 분리할 때 응집성을 깨지 않는다
  → 같은 정의(테이블, 정책 수치)가 두 파일에 나뉘면 분리하지 않는다

### 1.4 MUST 키워드 사용 기준

- 요구사항 문서에서는 두 축을 구분한다
  - `priority` (MoSCoW): 기능 전체가 이번 범위에 들어가는가 → must / should / could / won't
  - 수용 기준 강도: 기능이 들어갔을 때 해당 기준이 필수인가 → [MUST] / [SHOULD]
- 완료 판정과 연결한다
  - [MUST]가 하나라도 FAIL이면 미완료다
  - [SHOULD]를 충족하지 못하면 완료는 가능하지만 사유를 기록한다
  - MAY는 쓰지 않는다
    → 선택사항은 요구사항이 아니다
- 에이전트 규칙 문서에서는 위반이 곧 결함인 것에만 MUST / NEVER를 쓴다
  → 전부 MUST로 쓰면 에이전트가 우선순위를 구분하지 못한다
  - MUST 항목마다 검증 수단을 연결한다: 도구로 강제할 수 있으면 hook·CI, 없으면 검토자 체크리스트
  - 권장사항은 키워드 없이 "왜"와 함께 적는다
- 표기는 영어 대문자 키워드로 통일한다
  → grep과 검증 스크립트 파싱에 유리하다

### 1.5 프로젝트별 결정 항목

- 기술 스택에 따라 달라지는 항목은 하네스에서 값을 정하지 않고, 채우는 시점과 위치만 정한다
  → 프로젝트마다 새로 채운다

| 항목 | 채우는 시점 | 기록 위치 |
|---|---|---|
| UI 패키지 경로 (4.5) | 설계 착수 직후, UI 기초 작업 전 | `design/architecture.md` 모듈 경계 |
| 시크릿 저장소 도구 (5.3) | 구현 착수 전 | `design/architecture.md` 배포 단위 |
| 커밋 검사 도구 (5.4) | 구현 착수 전 | `docs/conventions.md` |
| `CLAUDE.md` 명령 (0.1) | 구현 착수 전 | `CLAUDE.md` |

- 각 결정은 DEC로 남긴다
- 구현 착수 전에 모두 채워져 있어야 한다 (1.1)

## 2. 전체 구조

```text
<project>/
  CLAUDE.md                 진입 문서 (0절)
  .claude/                  Claude Code 설정 전용
    skills/psw-*/           하네스 스킬 (10.3)
    agents/                 하위 에이전트 (9.1)
    scripts/psw/            검사 스크립트 (10.3)
    psw-version             설치한 하네스 버전
  docs/
    srs/                    요구사항 정의서 정본 (3.2)
    req/                    요구사항 상세 (3.3)
    design/                 설계 (4절)
      architecture.md
      security.md
      database/erd.md
      api/_conventions.md
      api/<domain>.md
      integration/<system>.md
      state/<entity>.md
      ui/
        ia.md
        ui-rules.md
        components.md
        screens/<domain>/<screen>.html
    ops/                    운영·유지보수 (6절, 자리만 확보)
    conventions.md          구현 규칙의 이유와 예외 (5.1)
    glossary.md             용어집 (8.4)
  records/                  경위 기록 (8절)
    decisions/              DEC (8.2)
    changes/                CR (7.3)
    open/                   OPEN (8.3)
    interviews/             인터뷰 원본
    references/             레퍼런스 조사 결과
    verifications/          직접 검증 기록 (5.2)
  <UI 패키지>/              토큰·공통 컴포넌트·레이아웃 정본 (4.5)
```

- `docs/`와 `records/`는 프로젝트 루트에 독립해서 둔다
  → `.claude/`는 도구 설정 전용이다. 문서는 사람, CI, 다른 도구도 읽는다
- `records/`는 `docs/` 밖에 둔다 (8절)
- 이 문서의 `srs/`, `req/`, `design/` 경로는 `docs/` 기준 상대 경로다
- `design/`은 설계를 뜻한다 (관습적 명칭)
- 번호 접두사(`01-`)는 순서가 의미 있는 SRS에만 쓴다

## 3. 기획

### 3.1 진행 흐름

1. 프로젝트 주제를 제시하고 관련 사례나 레퍼런스를 1~2개 참조한다
2. SRS 작성을 위해 프로젝트 방향성을 묻는 인터뷰를 진행한다
3. 인터뷰 결과로 SRS를 산출한다 (3.2)
4. SRS를 정본 삼아 REQ를 산출한다 (3.3~3.5)

- 에이전트는 경로별 `README.md`에서 참조 경로를 받고, 필요한 REQ만 읽는다
- 인터뷰 누락이나 방향 오류를 발견하면 피드백 루프로 되돌린다 (7절)
- 인터뷰 진행 규칙
  - 순서: 방향성 → 행위자 → 기능 → 비기능·보안·연동·데이터 → 제약
    → 기능이 나와야 비기능의 적용 범위가 정해진다
  - 한 번에 3~5개씩 질문한다
    → 한꺼번에 많이 물으면 답이 얕아진다
  - 질문에는 선택지와 추천안을 같이 제시한다
    → 사용자가 기획 전문가가 아니어도 답할 수 있다
  - 모르는 것은 OPEN으로 등록하고 다음 질문으로 진행한다
  - 해당 없는 것은 SRS에 "해당 없음"으로 명시한다
    → 빈칸이 "묻지 않음"인지 "필요 없음"인지 구분된다
  - 레퍼런스 사례의 기능을 후보로 제시하고, 사용자가 채택 여부를 고른다
    → 제외한 기능은 SRS `02-scope`의 범위 밖에 기록한다
  - 질문과 답의 원본은 `records/interviews/YYYY-MM-DD.md`에 남긴다
- 누락 검사
  - SRS 검토 시 체크리스트의 `[필수]` 항목마다 아래 중 하나가 있는지 확인한다
    - SRS에 반영된 답
    - `[OPEN-xxx]` 자리표시
    - "해당 없음"
  - 셋 다 없으면 누락이다
    → 피드백 루프의 트리거가 된다 (7.1)
- 체크리스트 위치
  - 하네스 저장소에 인터뷰 스킬의 참조 자료로 두고, 프로젝트에 복사하지 않는다
    → 모든 프로젝트가 같은 체크리스트를 쓰고, 프로젝트에는 결과(SRS, 인터뷰 원본)만 남는다
  - 질문 목록 초안은 부록 A에 둔다

### 3.2 SRS 구조와 소유 경계

- SRS는 크기 기준(1.3)에 맞춰 단일 파일이 아니라 폴더로 둔다
  → `srs/README.md`, `01-overview`, `02-scope`, `03-actors`, `04-requirements/`, `05-constraints`
- `04-requirements/`는 도메인별 파일로 나눈다 (예: `04-requirements/auth.md`)
  → 요구 130개를 한 파일에 두면 크기 기준을 넘는다
- SRS와 REQ의 소유 경계
  - SRS: 요구가 존재한다는 사실과 범위를 소유한다 (ID + 한 줄 + 우선순위)
  - REQ: 그 요구의 상세(흐름, 예외, 수용 기준)를 소유한다
- 개정 방식은 7.1을 따른다

### 3.3 요구사항 유형과 경로

| 유형 | 예시 | 경로 |
|---|---|---|
| 기능 | 로그인, 주문 | `req/functional/<domain>/` |
| 비기능 | 응답시간, 동시접속, 가용성 | `req/non-functional/` |
| 보안 | 인증 방식, 개인정보 마스킹, 감사 로그 | `req/security/` |
| 연동·외부 인터페이스 | 결제 PG 등 외부 API | `req/integration/` |
| 데이터 | 이관, 백업 | `req/data/` |
| 역할·권한 매트릭스 | 역할별 권한 | `req/actors.md` |
| 제약사항 | 기술 스택, 일정, 예산, 법규 | SRS `05-constraints` |

```text
req/
  README.md                          전체 인덱스
  functional/<domain>/               README.md, _policy.md, <feature>.md
  non-functional/
  security/
  integration/
  data/
  actors.md
```

- 기능 요구사항 1개당 md 1개를 산출한다
  → 기능 요구사항이 130개면 기능 REQ 파일도 130개다
- 역할 목록은 SRS `03-actors`가, 권한 매트릭스는 `req/actors.md`가 소유한다
- 제약사항은 상세가 따로 없으므로 REQ 파일을 두지 않는다
- 비기능·보안·연동·데이터는 영역별 파일 1개에 여러 항목을 담는다
  → 항목이 짧고 여러 기능에 걸치므로, 항목마다 파일을 만들면 파일 수만 늘어난다

| 폴더 | 파일 (예) | ID 영역 코드 |
|---|---|---|
| `non-functional/` | `performance.md`, `availability.md`, `compatibility.md`, `accessibility.md` | `PERF`, `AVAIL`, `COMPAT`, `A11Y` |
| `security/` | `authentication.md`, `authorization.md`, `privacy.md`, `audit.md` | `AUTH`, `AUTHZ`, `PRIV`, `AUDIT` |
| `integration/` | `<system>.md` (예: `pg.md`, `sms.md`) | `PG`, `SMS` |
| `data/` | `migration.md`, `retention.md`, `backup.md` | `MIG`, `RET`, `BACKUP` |

- 파일 이름과 영역 코드를 1:1로 맞춘다
  → `NFR-PERF-001`을 보면 `performance.md`에 있다는 것을 바로 알 수 있다
- 영역 코드는 도메인 코드와 함께 `req/README.md`에서만 정의한다 (1.2)
- 파일이 크기 기준(1.3)을 넘으면 영역 안에서 다시 나눈다
- 연동 요구사항 파일은 `design/integration/<system>.md`와 이름을 맞춘다
- 항목은 표 한 줄에 하나씩 적는다. 상태(`status`)는 파일 단위로 둔다 (1.1)

```markdown
| ID | 요구 | 기준 | 확인 방법 | 적용 범위 | 우선순위 |
|---|---|---|---|---|---|
| NFR-PERF-001 | 주문·결제 API 응답 시간 | p95 ≤ 500ms (동시 500명) | 부하 테스트 | 개별 | must |
| NFR-PERF-002 | 전체 페이지 첫 화면 표시 | LCP ≤ 2.5초 (모바일 4G) | 브라우저 측정 | 전체 | should |
| NFR-PERF-003 | 검색 응답 시간 | [OPEN-031] | 부하 테스트 | 개별 | should |
```

- 기준은 측정할 수 있는 수치로만 적는다. 정해지지 않았으면 `[OPEN-xxx]` 자리표시를 둔다
- 적용 범위는 전체와 개별로 나눈다

| 적용 범위 | 뜻 | 연결 방법 |
|---|---|---|
| 전체 | 모든 기능에 적용 | FR 파일에 적지 않는다. `req/README.md`에 전체 적용 목록으로 모은다 |
| 개별 | 특정 기능에만 적용 | FR 파일의 참조 요구사항 항목에만 적는다 |

  → 같은 연결을 양쪽에 적지 않는다 (1.2 단방향 원칙)
  → 구현자는 FR 파일과 `req/README.md`의 전체 적용 목록만 읽으면 된다

### 3.4 기능 REQ 크기 기준

- 기능 파일과 도메인 정책 파일(`_policy.md`)을 함께 쓴다
  → 잘게 분리하면 공유 규칙(만료 시간 등)이 여러 파일에 중복되고, 수정할 때 빠뜨릴 위험이 있다
  → 하나로 통합하면 읽는 양이 늘고, REQ 1개가 여러 행위를 덮어 추적성이 떨어진다
- 기능 파일 1개 = 사용자가 독립적으로 시작하고 끝내는 행위 1개
- 분리 조건
  - 트리거(행위자, 진입점)가 다를 때
  - 수용 기준을 따로 판정할 수 있을 때
  - 외부 연동이 달라 실패 흐름이 다를 때 (이메일 로그인 / 카카오 로그인)
  - 우선순위나 릴리스 시점이 다를 때
- 정책으로 빼는 조건
  - 사용자 행위가 아니라 규칙·수치일 때 (만료, 제한, 형식)
  - 둘 이상의 기능이 같이 참조할 때
- 정책의 위치
  - 도메인 안에서만 쓰이면 `req/functional/<domain>/_policy.md`
  - 여러 도메인에 걸치면 `req/security/`, `req/non-functional/`로 올린다

```text
req/functional/auth/
  README.md       인덱스
  _policy.md      세션 만료, 자동 로그인 기간, 동시 로그인 수 (수치의 유일한 정의처)
  login-email.md
  login-social.md
  logout.md
  auto-login.md
```

### 3.5 REQ 템플릿과 수용 기준

- REQ 파일 구성: ID, 출처(SRS), 행위자, 선행조건, 주 흐름, 예외 흐름, 수용 기준, 참조 요구사항(개별 적용 NFR·SEC·INT·DAT, 3.3), 우선순위, 상태
- 수용 기준이 핵심이다
  → 구현 완료 판정(5.2)과 테스트 진행의 기준이 된다
- 수용 기준의 강도 표기는 1.4를 따른다

## 4. 설계

### 4.1 설계 관점

- 아키텍처 설계
- 화면 설계: IA, 기능별 화면 시안
- 데이터베이스 설계
- API 설계
- 보안 설계: 인증·인가 구조, 데이터 보호
  → 보안 요구사항(`req/security/`)을 받는다
- 연동 설계: 외부 시스템 인터페이스와 실패 처리
- 상태 전이: 주문, 결제처럼 상태가 있는 엔터티
  → 버그가 가장 많이 나는 곳이다
- 설계 문서 공통 규칙
  - frontmatter에 `status`(1.1)와 `근거`(요구사항 ID, 1.2)를 적는다. ID가 있는 문서는 ID도 적는다
  - 섹션 순서: 목적·범위 → 본문 → 금지·제약 → 미결 자리표시
  - 다른 문서가 정본인 값은 적지 않고 참조한다

| 값 | 정본 |
|---|---|
| 상태값 | `design/state/<entity>.md` |
| 토큰, 컴포넌트 | UI 패키지 (4.5) |
| 정책 수치 | `req/functional/<domain>/_policy.md` |
| 역할별 권한 | `req/actors.md` |

  - 예외: API 문서는 허용 역할을 적는다
    → 구현 에이전트가 API 문서 하나만 읽고 권한 검사를 넣을 수 있다. 어긋남은 교차 검증(4.3)으로 잡는다
  - HTML 목업은 frontmatter 대신 파일 맨 위 주석에 메타 정보를 적는다
    → 예: `<!-- id: SCR-ORD-001 | 근거: FR-ORD-001 | API: API-ORD-001 | status: draft -->`
  - 목업은 기본, 빈 상태, 로딩, 오류 네 가지 상태를 보여준다
- 문서별 필수 항목은 부록 B에 둔다

### 4.2 설계 문서의 축

- REQ는 기능별로 두지만, 설계 문서는 관심사에 맞는 축으로 나눈다
  - 전역: 아키텍처, 보안, ERD, IA
    → 기능별로 쪼개면 테이블 정의 같은 전역 정의가 여러 곳에 흩어진다
  - 도메인별: 화면, API
  - 외부 시스템별: 연동
  - 엔터티별: 상태 전이 (4.6)

### 4.3 설계 순서와 교차 검증

- 아키텍처 → UI 기초(4.5) → DB · API · 화면 순서로 진행하고, DB · API · 화면은 서로 맞물려 진행한다
  → 순서를 고정하면 뒤 단계에서 앞 단계를 계속 고치게 된다
  → UI 기초를 만들려면 기술 스택이 SRS `05-constraints`에서 먼저 정해져야 한다
- 설계 루프 안에 DB · API · 화면 세 문서의 교차 검증 단계를 둔다
- 설계는 기획과 별개로 자체 피드백 루프를 반복한다 (7절)
- 교차 검증 항목

| # | 구분 | 항목 | 검사 방식 |
|---|---|---|---|
| 1 | 누락 | 승인된 모든 `FR`이 하나 이상의 `SCR` 또는 `API`에서 근거로 참조된다 | 스크립트 |
| 2 | 누락 | 모든 `NFR` / `SEC` / `INT`가 설계 문서 어딘가에서 근거로 참조된다 | 스크립트 |
| 3 | 일치 | 목업 메타의 API ID가 API 문서에 있다 | 스크립트 |
| 4 | 일치 | API 요청·응답 필드가 ERD 컬럼과 대응한다 (파생 필드는 명시) | 검토자 |
| 5 | 일치 | API가 유발하는 상태 전이가 state 전이 표에 있고, 사용자 이벤트마다 대응하는 API가 있다 | 검토자 |
| 6 | 일치 | API 허용 역할과 IA 접근 역할이 `actors.md`와 일치한다 | 검토자 |
| 7 | 일치 | ERD 상태 컬럼이 state 문서를 참조한다 | 스크립트 |
| 8 | 일치 | 목업이 `components.md`에 있는 컴포넌트만 쓴다 | 스크립트 |
| 9 | 일치 | 연동 실패 처리가 API 오류 응답이나 상태 전이에 반영돼 있다 | 검토자 |
| 10 | 고아 | 근거가 비어 있는 `SCR` / `API`가 없다 | 스크립트 |
| 11 | 고아 | 어떤 API도 쓰지 않는 테이블, 어떤 화면도 쓰지 않는 API가 없다 (경고만) | 스크립트 |
| 12 | 고아 | 존재하지 않는 ID를 참조하지 않는다 | 스크립트 |

- 교차 검증은 설계 루프를 돌 때마다, 그리고 설계 문서를 `approved`로 올리기 전에 실행한다
- 불일치 처리
  - 설계 문제: 설계 문서를 `draft`에서 고친다
  - 요구사항 문제: 기획 루프를 호출한다 (7.2)
  - 근거가 빈 설계 요소: 범위 밖 작업을 의심한다

### 4.4 화면 시안 형식

- 화면 시안은 HTML 목업으로 작성한다
  → 이미지만 두면 에이전트가 읽지 못한다
- 목업은 UI 패키지의 빌드된 CSS(토큰, 컴포넌트 클래스)를 불러 쓴다 (4.5)
- 목업은 `components.md`에 있는 컴포넌트만 쓴다
  → 새 컴포넌트가 필요하면 UI 패키지에 먼저 추가한다
- 컴포넌트가 바뀌면 기존 목업 모양도 바뀐다
  → 승인 시점의 모양을 보존해야 하면 스크린샷을 records에 남긴다

### 4.5 UI 정본 (토큰·공통 컴포넌트·레이아웃)

- 토큰, 공통 컴포넌트, 레이아웃의 정본은 코드 쪽 UI 패키지 하나다
  → 목업과 프론트가 같은 정본을 가져다 쓰므로 서로 어긋나지 않는다
- 토큰: `tokens.json`을 정본으로 두고, 빌드해서 CSS 변수·TS 상수·테마를 만든다
- 컴포넌트: 스타일을 CSS 클래스 층으로 분리한다
  → 목업은 클래스를, 프론트는 클래스를 감싼 컴포넌트를 쓴다
- 레이아웃: 레이아웃 프리미티브와 토큰의 브레이크포인트로 정의한다
- `design/ui/` 문서에는 값을 적지 않는다
  - `ui-rules.md`: 사용 규칙과 이유
  - `components.md`: 이름 → 경로 → 용도 목록 (가능하면 소스에서 자동 생성)
- UI 패키지 경로 (프로젝트별 결정, 1.5)
- 하네스는 UI 패키지 시작 템플릿을 제공한다. 템플릿 내용 (구축 단계, 10.4)

### 4.6 상태 전이 문서

- 상태 컬럼을 가진 엔터티 1개당 `design/state/<entity>.md` 1개를 둔다
  → 여러 기능이 같은 상태를 바꾸므로, 기능별 문서에 나누면 전이 규칙이 어긋난다
- 대상: 상태가 3개 이상이거나, 시간·외부 이벤트로 상태가 바뀌는 엔터티
- 상태값의 정본은 이 문서다. ERD는 이 문서를 참조한다
- 구성: 상태, 전이(현재·이벤트·다음·조건·주체·부수 효과·근거), 금지 전이, 다른 엔터티와의 연동, 다이어그램
- 전이 표의 각 행은 AC 테스트(5.2)와 API 교차 검증의 기준이 된다

## 5. 구현

### 5.1 규칙과 강제 수단

- 구현 규칙: lint, git 규칙, convention, 주석 규칙
- 규칙은 기계로 강제한다
  - lint와 포맷: 설정 파일
  - git 규칙: hook 또는 CI
- 규칙 문서는 `docs/conventions.md` 한 파일에 두고, "왜"와 예외만 남긴다
  → 문서에 규칙을 다시 적으면 설정 파일과 어긋난다
- MUST 항목의 검증 수단 연결은 1.4를 따른다
- 기술 스택별 도구는 스택의 표준 도구를 쓴다 (예: ESLint + Prettier, Ruff)

#### lint·포맷

- lint는 error만 차단한다. warning은 막지 않는다
- 규칙을 끄는 주석을 쓸 때는 같은 줄에 사유를 적는다
- 포맷은 커밋 전에 자동으로 적용한다

#### git

| 규칙 | 내용 |
|---|---|
| main 보호 | main에 직접 커밋하지 않는다 |
| 브랜치 이름 | `<type>/<ID>-<요약>` (예: `feat/FR-ORD-010-guest-checkout`) |
| 브랜치 단위 | 브랜치 1개 = 요구사항 1개 또는 CR 1개 |
| 병합 방식 | 개별 커밋을 유지한다 (rebase 또는 merge). squash하지 않는다 |
| 금지 | main에 force push |

- squash하지 않는 이유: 커밋별 트레일러(5.4)가 뭉개져 추적이 끊긴다

#### convention

- 코드 이름은 용어집(`glossary.md`)의 영문명을 따른다
  → 같은 개념이 여러 이름으로 갈라지는 것을 막는다
- 디렉터리 구조와 의존 방향은 `architecture.md`의 모듈 경계를 따른다. 가능하면 도구로 검사한다

#### 주석

- 공개 함수와 모듈에는 문서 주석을 필수로 단다 (lint로 강제)
- 내부 코드에는 필요할 때만 "왜"를 적는다. "무엇"은 코드가 설명한다
- 요구사항 ID는 적지 않는다 (1.2)
- `TODO`에는 참조를 붙인다 (예: `TODO(OPEN-021)`). 참조 없는 TODO는 쓰지 않는다
- 주석 처리한 코드는 남기지 않는다
- 임시 우회에는 사유와 해제 조건을 함께 적는다
- 주석은 한국어로 쓴다

```ts
/**
 * 비회원 주문을 생성한다.
 * 휴대폰 인증이 끝난 요청만 받는다.
 * @throws VerificationRequiredError 인증 토큰이 없거나 만료된 경우
 */
export function createGuestOrder(input: GuestOrderInput): Order { ... }

// PG사가 동일 주문번호 재요청을 중복 결제로 처리하므로, 재시도할 때는 새 멱등 키를 쓴다
const idempotencyKey = crypto.randomUUID();

// TODO(OPEN-012): 동시 로그인 허용 기기 수가 정해지면 제한 로직 추가

// eslint-disable-next-line @typescript-eslint/no-explicit-any -- PG SDK가 타입 정의를 제공하지 않음
const response: any = await pgClient.approve(payload);

// 택배사 API가 주말에 배송 완료 이벤트를 늦게 보내므로, 72시간 뒤 자동으로 완료 처리한다.
// 택배사 웹훅이 실시간으로 바뀌면 제거한다.
```

| 금지 예 | 이유 |
|---|---|
| `// 주문 상태를 PAID로 바꾼다` | 코드가 이미 설명하는 "무엇"을 되풀이한다 |
| `// TODO: 동시 로그인 제한 확인` | 참조가 없어 추적할 수 없다 |
| `// FR-ORD-010 비회원 주문 처리` | 요구사항 ID는 커밋 트레일러에만 적는다 |
| `// const discount = calcLegacyDiscount(order);` | 이전 코드는 git에 있다 |

### 5.2 완료 정의(DoD)

- REQ 수용 기준 충족 (판정 규칙은 1.4)
- AC 테스트 통과
- 직접 검증 AC 통과 (검증 기록)
- lint 통과
- 주석 작성 및 규칙 통과
- 요구사항 ID 커밋 태깅 (5.4)

#### 검증 방식

- AC 성격에 따라 검증 방법을 나눈다

| AC 성격 | 검증 방법 | 코드 산출물 |
|---|---|---|
| API·데이터로 확인 가능 | AC 테스트 코드 (API 통합 테스트) | 있음 |
| 화면으로만 확인 가능 (문구, 배치, 표시 여부) | 에이전트 직접 검증 + 검증 기록 | 없음 |
| 상태 전이 표의 각 행 (4.6) | AC 테스트 코드 | 있음 |

- AC 테스트는 사용자가 보는 동작을 API나 화면 수준에서 확인한다
  → 내부 함수 단위로 쪼개면 AC와 1:1로 대응되지 않는다
- 내부 함수 단위 테스트는 만들지 않는다
- `[MUST]` AC는 테스트나 직접 검증이 필수다. `[SHOULD]` AC의 테스트 코드는 선택이다
- AC 테스트는 구현 코드보다 먼저 작성한다
  → 구현에 맞춰 테스트를 쓰는 것을 막는다
- 테스트 이름에 AC ID를 단다 (예: `FR-ORD-010 AC-1: 비회원 주문 생성`)
- MUST: 테스트를 통과시키려고 테스트를 고치지 않는다
  → 요구사항이 바뀐 것이면 CR(7.3)을 거친다
- 검증 기록은 직접 검증한 AC에만 남긴다
  - 위치: `records/verifications/<FR-ID>.md`
  - 항목: 대상 커밋, 검증일, AC별 결과·방법·증거
  - 다시 검증하면 최신 결과로 덮어쓴다. 이전 결과는 git에 남는다
- 검증 중 만든 임시 스크립트는 커밋하지 않는다
  → 저장소에 남기면 사실상 테스트 코드가 된다
- 코드가 바뀌면 ID 추적(1.2)으로 영향받는 기능을 찾아, 직접 검증 AC를 다시 검증한다
  → AC 테스트 코드는 CI가 자동으로 다시 확인한다
- AC 테스트는 검증자가 구현 전에 작성한다 (9.1)

### 5.3 환경변수·시크릿 관리

- **에이전트 규칙**
  - **NEVER: 실제 시크릿 값을 읽거나 출력하지 않는다**
  - **NEVER: 시크릿 값을 로그, 문서, 커밋, 테스트 데이터에 넣지 않는다**
  - **MUST: 시크릿이 필요하면 사용자에게 설정을 요청한다**
- 커밋 금지
  - NEVER: 시크릿을 저장소에 커밋하지 않는다
  - 커밋 전 스캔과 CI 스캔으로 강제한다 (예: gitleaks)
- `.env`
  - git에서 제외한다
  - `.env.example`에 키 이름만 적어 커밋한다
    → 필요한 환경변수 목록의 정본이다
- 환경 구분: local / dev / prod
  - 배포 환경의 값은 시크릿 저장소에 둔다. 도구 (프로젝트별 결정, 1.5)
- 코드 접근
  - 환경변수는 설정 모듈 한 곳에서만 읽는다
  - 앱 시작 시 검증하고, 누락되면 바로 실패한다
- 유출 시 즉시 폐기하고 재발급한다
  → git 이력을 지우는 것으로는 해결되지 않는다
- 외부 연동 키의 준비 상태는 연동 문서(`integration/<system>.md`)의 테스트 환경 항목과 연결한다

### 5.4 커밋 메시지 규칙

```text
feat: 비회원 주문 추가

비회원도 휴대폰 인증으로 주문을 조회할 수 있게 한다.

Refs: FR-ORD-010, FR-ORD-004
Decision: DEC-0012
CR: CR-003
Closes: OPEN-021
```

- 형식: `type: 제목` + 본문(선택) + 트레일러. scope는 쓰지 않는다
- type: `feat`, `fix`, `refactor`, `test`, `docs`, `chore`, `build`, `ci`
- 제목은 한국어로, 무엇을 했는지 적는다
- 길이 (문자 개수 기준)

| 대상 | 권장 | 차단 |
|---|---|---|
| 제목 (type 포함) | 50자 이내 | 72자 초과 |
| 본문 한 줄 | - | 없음 (100자 초과 시 경고만) |
| 본문 전체 | "무엇을, 왜"를 몇 줄로 | 없음 |
| 트레일러 줄 | - | 없음 |

  → 긴 설명이 필요하면 DEC나 PR 본문에 적는다
- 트레일러

| 키 | 값 | 의미 |
|---|---|---|
| `Refs:` | 요구사항 ID (`FR-`, `NFR-` 등) | 이 커밋이 다루는 요구사항 |
| `Decision:` | `DEC-NNNN` | 근거가 된 결정 |
| `CR:` | `CR-NNN` | 반영한 변경 요청 |
| `Closes:` | `OPEN-NNN` | 해결한 미결 |
| `Role:` | `implementer`, `verifier` | 커밋을 만든 하위 에이전트 (9.4) |

- ID는 제목에 넣지 않고 트레일러에만 적는다
  → 제목이 읽기 쉽고, `git interpret-trailers`로 기계가 뽑아낼 수 있다
- 트레일러 필수 조건

| 변경 대상 | 필수 트레일러 |
|---|---|
| 코드 + `feat` / `fix` | `Refs:` |
| 코드 + 그 외 type | 없음 |
| `docs/srs/`, `docs/req/`, `docs/design/` | `Refs:` 또는 `Decision:` 중 하나 이상 |
| `approved` 문서의 내용 변경 | `CR:` + `Decision:` (7.3) |
| `records/` | 없음 |
| 하위 에이전트가 만든 커밋 | `Role:` |

- 강제 수단
  - commit-msg hook: 형식, 길이, 필수 트레일러, ID 존재 여부를 검사한다
  - CI: 같은 검사를 다시 실행한다
    → hook은 `--no-verify`로 건너뛸 수 있다
  - 도구 (프로젝트별 결정, 1.5). 검사 스크립트는 하네스가 제공한다

## 6. 운영·유지보수

- 대상: 배포, 모니터링, 장애 대응
- 최소 구조에서는 제외하되, 디렉터리 자리를 미리 확보한다

## 7. 피드백 루프

- 인터뷰 누락이나 잘못된 방향을 발견하면 인터뷰를 다시 진행하고, SRS를 개정한 뒤 REQ 산출까지 다시 진행한다
- 기획과 설계가 끝날 때까지 각 단계에서 반복 호출한다

### 7.1 4요소

| 요소 | 기획 루프 | 설계 루프 |
|---|---|---|
| 트리거 | 누락, 모순, 실현 불가능, 사용자 번복 | 교차 검증 불일치(4.3), 설계 문서 간 모순, 기술적 실현 불가능, 구현 중 설계 결함 발견, 사용자 번복 |
| 개정 방식 | 최신만 정본 + git. 승인 시 SRS 태그 | 최신만 정본 + git. 태그 없이 파일별 `approved`로 승인 |
| 영향 분석 | ID로 영향받는 REQ만 다시 산출 | 바뀐 설계 요소를 참조하는 문서만 다시 검토 |
| 종료 조건 | OPEN 0건, pending CR 0건 | 도메인 단위로 교차 검증 통과, OPEN 0건, pending CR 0건, 해당 설계 문서 모두 `approved` |

- 개정 방식 공통: 최신 상태만 정본으로 두고, 이력은 git에 둔다. 변경 요청은 CR(7.3), 결정 경위는 DEC(8.2)에 둔다
  - 탈락안
    - 파일 내 이력 절 누적 → 파일이 계속 커진다
    - 버전별 전체 사본(`v1.0/`, `v1.1/`) → 옛 버전을 잘못 읽을 위험이 있고, 검색 결과가 오염된다
    - 원본 + 증분 부록 → 에이전트가 전부 읽고 병합해야 해서 토큰 소모와 모순 위험이 가장 크다
  - SRS는 승인 시점에 git 태그(`srs-v1.1`)를 찍고, 그 시점의 md에서 PDF를 생성한다
  - 설계는 태그를 찍지 않는다
    → 도메인마다 끝나는 시점이 다르고, 구현 착수 조건(1.1)이 파일별 `approved`를 본다
- 종료 조건 공통
  - 남은 미결을 모두 "보류로 수용"한 경우도 종료로 본다
    → 종료 조건이 없으면 루프가 끝나지 않는다
  - `[OPEN-` 검색 결과 0건으로 확인한다 (8.3)
  - `status: pending`인 CR이 0건이어야 한다 (7.3)
- 설계 루프는 도메인 단위로 종료한다
  → 한 도메인의 설계가 끝나면 다른 도메인을 기다리지 않고 구현을 시작할 수 있다
- 설계 루프의 영향 분석 경로

| 바뀐 것 | 다시 볼 곳 |
|---|---|
| ERD (테이블, 컬럼) | 해당 필드를 쓰는 API 문서, state 문서 |
| state (상태, 전이) | 상태 전이를 유발하는 API, 해당 목업 |
| API | 목업 메타에서 그 API ID를 쓰는 화면, 관련 AC 테스트와 검증 기록 |
| 컴포넌트 | 목업 (UI 패키지를 참조하므로 자동 반영) |
| 연동 | 실패 처리를 반영한 API, state 문서 |

- 교차 검증의 스크립트 항목은 매번 전체를 실행하고, 검토자 항목은 영향받은 문서만 다시 본다
- 반복 한도: 같은 불일치가 2회 이상 다시 나오면 요구사항 문제로 보고 사용자에게 보고한다
  → 설계 루프가 헛돌지 않게 한다

### 7.2 단계 사이 루프 규칙

- 설계 중 기획 결함을 발견하면 설계 단계에서 요구사항을 고치지 않는다
- 기획 루프를 호출해 SRS → REQ 순서로 반영한 뒤 설계로 돌아간다
  → 하위 문서에서 상위 결정을 변경하지 않는다 (1.1)
- 구현 중 설계 결함을 발견하면 구현 단계에서 설계를 고치지 않는다
- 설계 루프를 호출해 설계 문서에 반영한 뒤 구현으로 돌아간다

```text
기획 루프 ◀── 요구 결함 ── 설계 루프 ◀── 설계 결함 ── 구현
```

### 7.3 변경 요청(CR, Change Request)

- 역할 구분
  - OPEN: 아직 정해지지 않은 것 (공백)
  - CR: 이미 승인된 것을 바꾸자는 요청
  - DEC: 무엇을 왜 그렇게 정했는가
- 사용 기준
  - `draft` 문서 수정: CR 없이 수정한다 (결정이 끼면 DEC만 작성)
  - 누락·모호함 발견: OPEN
  - 승인된 내용을 바꾸거나 없애야 함 (트리거: 모순, 실현 불가능, 사용자 번복): CR
  - OPEN의 답으로 승인된 문서에 내용을 추가함: CR 없이 반영하고 `approved → draft` 규칙(1.1)으로 재승인받는다
- `records/changes/CR-xxx.md`에 짧게 남긴다
- 항목: 요청자, 트리거, 변경 대상 ID, 요청 내용, 사유(근거 인터뷰 포함), 영향 범위
- 처리
  - 작성: `status: pending`으로 둔다
  - 수락: DEC를 작성하고, 원천에 반영한 뒤 CR 파일을 삭제한다
    → 요청 원문은 git 이력에 남고, DEC가 `CR-xxx`를 참조한다
  - 거절: `status: rejected`로 바꾸고 반려 사유를 적어 남겨둔다
    → 같은 요청이 다시 올라올 때 근거가 된다
- MUST: 수락과 거절은 사용자만 정한다
- CR ID는 재사용하지 않는다
- 반영 커밋에는 트레일러 `CR: CR-xxx`를 단다

## 8. records

- 결정사항, 미결사항, 인터뷰 원본 같은 경위 기록은 REQ·설계와 분리된 공간에서 관리한다
  → REQ에 섞이면 이력이 꼬이고 문서가 난잡해진다

### 8.1 3층 분리

| 무엇 | 위치 |
|---|---|
| 지금 값 (결정의 결과) | 원천 문서 (REQ, 설계) |
| 왜 (맥락, 대안, 결정자) | `records/decisions/DEC-xxxx.md` |
| 언제 무엇에서 무엇으로 바뀌었나 | git (+ CR) |

- 원천 문서에는 값과 근거 ID만 적는다
  → 예: `유휴 세션 만료: 60분 (DEC-0007)`
  → 구현 에이전트는 원천만 읽어도 작업할 수 있어야 하고, DEC는 이유가 궁금할 때만 따라간다

### 8.2 결정 기록

- 결정 기록은 대체 관계를 표시한다 (예: DEC-0007이 DEC-0003을 대체)

### 8.3 미결사항 수명주기와 자리표시

- 수명주기: 발생 → (인터뷰로) 해결 → 결정 기록으로 이관 + 원천 문서 반영 → 미결 닫기
  → 이 흐름이 피드백 루프의 종료 조건(7.1)과 연결된다
- 미결사항은 원천에 자리표시만 두고 내용은 records에 둔다
  → 예: `동시 로그인: 1기기 [OPEN-012]`
- 해결되면 자리표시를 값과 DEC ID로 바꾸고 OPEN을 닫는다
- DEC가 생성되면 OPEN 파일을 삭제해 닫는다 (CR 수락과 같은 방식, 7.3)
  → `records/open/`에 남은 파일은 모두 아직 답이 없는 질문이다
  → 질문 원문은 git 이력에 남고, DEC가 `OPEN-xxx`를 참조한다
- OPEN ID는 재사용하지 않는다

### 8.4 용어집 위치

- 용어집은 records가 아니라 원천 쪽(`glossary.md`)에 둔다
  → 경위가 아니라 코드 네이밍까지 좌우하는 정본이다

## 9. 확장: 하위 에이전트

- 역할별 에이전트를 두고, 작업 대상 FR 경로를 컨텍스트로 넘긴다

### 9.1 역할과 모델

| 역할 | 하는 일 | 권한 | 하지 않는 일 | 모델 |
|---|---|---|---|---|
| 오케스트레이터 (메인 세션) | 작업 분배, 반복 횟수 관리, 에스컬레이션, 사용자 보고 | 전체 | 직접 구현 | 메인 세션 모델 |
| 구현자 | FR 1개 구현 | 편집 (FR별 worktree) | REQ·설계·테스트 파일 수정, 범위 밖 파일 수정 | `opus` |
| 검토자 | 설계 검토(4.3의 검토자 항목), 코드 검토(설계 정합, 규칙 위반, 범위 이탈, 보안) | 읽기 전용 | 직접 수정 | `opus` |
| 검증자 | 구현 전 AC 테스트 작성, 구현 후 테스트 실행·화면 AC 직접 검증·검증 기록 작성 | 테스트 파일과 `records/verifications/`만 편집 | 구현 코드 수정, 통과시키려고 테스트 수정 | `opus` |

- 검토자는 설계용·코드용 체크리스트를 함께 가진 하나의 에이전트다
  → 둘 다 읽기만 하고 지적한다는 성격이 같다
- AC 테스트는 검증자가 구현 전에 작성하고, 구현자는 통과시키기만 한다
  → 테스트를 쓰는 쪽과 통과시키는 쪽을 나눠야 검증이 독립적이다
- 모델은 버전 없이 별칭으로 지정한다
  → 새 모델이 나와도 에이전트 파일을 고치지 않는다
- 처음 운영하는 하네스이므로 세 역할 모두 `opus`로 시작한다
  → 운영해 보고 비용 대비 품질을 확인한 뒤 역할별 조정을 검토한다
- 동시에 실행하는 구현자는 2개다
- 에이전트 파일은 오케스트레이터를 뺀 3개(`implementer.md`, `reviewer.md`, `verifier.md`)다
  - 하네스 저장소에 두고, 프로젝트 초기화 때 `.claude/agents/`로 복사한다
  - 설계서 확정 후 구축 단계에서 스킬과 함께 만든다

### 9.2 흐름

```text
오케스트레이터
  → 검증자: FR의 AC로 테스트 작성 (구현 전)
  → 구현자: worktree에서 구현
       └ 설계·요구 결함 발견 → 즉시 중단하고 보고 → 해당 루프 호출 (7.2)
  → lint·hook (도구 검사)
  → 검토자: 코드 검토 ── 수정 필요 → 구현자로
  → 검증자: 테스트 실행 + 화면 AC 직접 검증 + 검증 기록 ── FAIL → 구현자로
  → 사용자 확인 (FR마다) → 병합
```

- 병합 전 사용자 확인은 FR마다 받는다
- 확인 요청은 아래 요약 형식으로 한다

```text
[병합 확인] FR-ORD-010 비회원 주문
- 변경 파일: 5개 (코드 3, 테스트 2)
- AC 결과: AC-1 PASS(테스트) / AC-2 PASS(직접 검증) / AC-3 FAIL [SHOULD] 사유: SMS 키 미준비
- 검토 지적: 2건 → 모두 수정됨
- 남은 OPEN·CR: 없음
→ 병합할까요?
```

### 9.3 원칙

| 원칙 | 내용 |
|---|---|
| 컨텍스트 독립 | 검토자와 검증자에게는 구현자의 설명을 주지 않고, 산출물과 원천 문서만 준다 |
| 지적과 수정 분리 | 지적하는 쪽은 고치지 않는다 |
| 근거 ID 필수 | 모든 지적에 근거 ID를 단다. 근거가 없으면 "제안"으로 분류한다 |
| 반복 한도 | 같은 AC가 3회 FAIL하면 설계·요구 문제로 보고 사용자에게 보고한다 |
| 병렬 구현 | FR마다 worktree를 나눈다. 공유 파일(DB 마이그레이션, 라우트 등록, UI 패키지)은 구현자가 고치지 않고 병합 단계에서 순서대로 반영한다 |
| 항상 분리 | 작은 작업이라도 검토와 검증은 별도 에이전트가 한다 |

- 컨텍스트 독립: 구현자의 논리에 설득되면 결함을 놓친다
- 지적과 수정 분리: 스스로 고치면 기준이 낮아진다
- 반복 한도가 설계 루프(2회, 7.1)보다 1회 많은 이유: 구현은 시행착오가 더 많다

### 9.4 권한 강제

- 도구 제한: 에이전트 파일의 `tools` 필드로 막는다. 검토자에게는 읽기 도구만 준다
  → 검토자에게 Bash를 주면 파일을 고칠 수 있으므로, diff는 오케스트레이터가 뽑아서 넘긴다
- 경로 제한: `tools` 필드로는 경로 단위 제한이 안 된다
  - 지시로 제한한다
  - 하위 에이전트의 커밋에는 트레일러 `Role:`을 단다 (5.4)
  - 병합 전에 스크립트가 `Role`별 허용 경로와 커밋의 변경 경로를 비교한다

| Role | 허용 경로 |
|---|---|
| `implementer` | 구현 코드 (테스트 파일, `docs/`, `records/` 제외) |
| `verifier` | 테스트 파일, `records/verifications/` |

  - 편집 시점에 hook으로 막는 방법은 hook이 요청한 에이전트를 구분할 수 있는지 확인한 뒤 추가한다 (구축 단계, 10.4)

## 10. 하네스 저장소 구성

### 10.1 저장소 구조

```text
agent-harness-standards/
  README.md                 저장소 소개, 설치 방법
  harness-psw.md            설계서 (정본)
  install.sh                프로젝트에 스킬·에이전트·스크립트를 복사
  skills/
    <skill>/
      SKILL.md
      references/           그 스킬이 만드는 문서의 템플릿, 체크리스트
      templates/            그 스킬이 프로젝트에 복사하는 파일
  agents/                   implementer.md, reviewer.md, verifier.md
  scripts/                  검사 스크립트
```

- 템플릿은 그 파일을 만드는 스킬이 소유한다
  - 문서 템플릿, 체크리스트: `references/`
  - 프로젝트에 그대로 복사하는 파일: `templates/` (예: `psw-init`의 프로젝트 골격, `psw-design`의 UI 패키지 시작 템플릿)
  → 한곳에 모아두면 스킬과 템플릿이 따로 논다
  → 설치 후에는 프로젝트에 복사된 스킬만 접근할 수 있으므로, 템플릿이 스킬 밖에 있으면 찾을 수 없다
- 여러 스킬이 함께 쓰는 것(스크립트, 에이전트)만 최상위에 둔다

### 10.2 스킬 목록

- 기존 스킬과 이름이 겹치지 않도록 `psw-` 접두사를 쓴다

| 단계 | 스킬 | 하는 일 | 설계서 |
|---|---|---|---|
| 준비 | `psw-init` | 프로젝트 골격 생성 (`CLAUDE.md`, `docs/`, `records/`, `glossary.md`, `.env.example`, `.gitignore`) | 0.1, 2절 |
| 기획 | `psw-interview` | 주제·레퍼런스 제시, 인터뷰 진행, 원본 기록, OPEN 등록 | 3.1, 부록 A |
| 기획 | `psw-srs` | SRS 작성, 누락 검사, 기준선 태그, PDF 생성 | 3.2 |
| 기획 | `psw-req` | SRS → REQ 파생 (FR, `_policy`, 영역 파일, README 인덱스) | 3.3~3.5 |
| 설계 | `psw-design` | 프로젝트별 결정 항목 채우기, 설계 문서·UI 기초·목업 작성 | 1.5, 4절, 부록 B |
| 설계 | `psw-crosscheck` | 교차 검증 실행 (스크립트 + 검토자) | 4.3 |
| 구현 | `psw-implement` | FR 단위 흐름 진행 (검증자 → 구현자 → 검토자 → 검증자 → 병합 확인) | 5절, 9절 |
| 공통 | `psw-change` | 피드백 루프: OPEN·CR·DEC 작성과 처리, 영향 분석, 종료 조건 확인 | 7절, 8절 |

- `psw-design`은 문서별 템플릿을 `references/`로 나눠 하나의 스킬로 둔다. 커지면 UI 부분을 `psw-ui`로 분리한다
- `psw-change`는 기획·설계·구현 모두에서 호출된다
  → 각 스킬에 같은 절차를 복사하지 않는다
- 진행 현황과 RTM은 스크립트로 만들고, 스킬로 두지 않는다

### 10.3 설치

- 전역에 설치하지 않는다. `install.sh <프로젝트 경로>`로 프로젝트에 복사한다
  → 전역에 설치하면 모든 프로젝트에 걸려 다른 방법론과 섞인다
  → `psw-init`도 프로젝트에 먼저 있어야 실행할 수 있으므로, 첫 설치는 스크립트로 한다
- 복사 위치
  - 스킬: `.claude/skills/psw-*/`
  - 에이전트: `.claude/agents/`
  - 검사 스크립트: `.claude/scripts/psw/`
- 다시 설치하면 하네스가 소유한 파일(`psw-*` 스킬, 하네스 에이전트 3개, `scripts/psw/`)만 교체하고, 프로젝트의 다른 파일은 건드리지 않는다
- 설치한 하네스 버전(커밋 해시)을 `.claude/psw-version`에 남긴다
- hook·CI 설정은 설치 시 넣지 않는다
  → 커밋 검사 도구가 프로젝트별 결정 항목(1.5)이므로 구현 착수 전에 설정한다

### 10.4 구축 단계에서 정할 것

- 형식이 정해지지 않은 템플릿
  - `req/actors.md` 권한 매트릭스
  - DEC 항목
  - OPEN 항목
  - 인터뷰 원본, 레퍼런스 조사
  - `glossary.md`
- UI 패키지 시작 템플릿 내용 (4.5)
- 편집 시점 경로 차단 hook의 가능 여부 (9.4)
- 검사 스크립트
  - 커밋 메시지 검사 (5.4)
  - 교차 검증 (4.3)
  - 루프 종료 조건 확인 (7.1)
  - RTM 생성 (1.2)
  - 문서 크기 신호 (1.3)
  - 역할별 경로 검사 (9.4)
  - `approved` 변경 검사 (1.1)

## 부록 A. 인터뷰 질문 목록 (초안)

> 인터뷰 스킬을 만들 때 참조 자료로 옮긴다 (3.1).
> `[필수]`는 모든 프로젝트에서 묻고, `[선택]`은 프로젝트 성격에 따라 묻는다.
> 운영(배포, 모니터링)은 자리만 확보하므로(6절) 제외한다.

### A.0 방향성 → SRS `01-overview`, `02-scope`

- [필수] 어떤 문제를 해결하나, 누구를 위한 것인가
- [필수] 성공 기준은 무엇인가 (측정 가능한 수치로)
- [필수] 이번 범위에 넣을 것 / 뺄 것
- [필수] 첫 출시(MVP)에 꼭 있어야 하는 기능
- [선택] 레퍼런스와 다르게 가져갈 점
- [선택] 주요 일정 (출시일, 중간 점검)

### A.1 행위자·권한 → SRS `03-actors`, `req/actors.md`

- [필수] 사용자 역할 목록 (비회원, 회원, 관리자 등)
- [필수] 역할별로 할 수 있는 것 / 없는 것
- [필수] 로그인 없이 쓸 수 있는 기능
- [선택] 관리자 등급 구분, 권한 위임

### A.2 기능 → SRS `04-requirements/`, `req/functional/`

- [필수] 도메인 목록 (회원, 주문, 결제 등)
- [필수] 도메인별 핵심 행위
- [필수] 행위별 주요 흐름과 실패·취소 시 처리
- [선택] 알림 (이메일, SMS, 푸시)
- [선택] 검색, 필터, 정렬
- [선택] 파일 업로드, 다운로드

### A.3 비기능 → `req/non-functional/`

- [필수] 예상 사용자 수, 동시 접속자 수
- [필수] 지원 환경 (브라우저, 모바일, 앱)
- [선택] 응답 시간 목표
- [선택] 가용성 목표, 점검 시간 허용 여부
- [선택] 접근성, 다국어, 시간대

### A.4 보안 → `req/security/`

- [필수] 인증 방식 (이메일, 소셜, SSO, 2단계 인증)
- [필수] 수집하는 개인정보 항목
- [선택] 세션 정책 (만료 시간, 동시 로그인)
- [선택] 마스킹, 암호화 대상
- [선택] 감사 로그 대상 (누가 언제 무엇을)
- [선택] 비밀번호 정책

### A.5 연동 → `req/integration/`

- [필수] 외부 시스템 목록 (결제, 본인인증, 지도, 메일, SMS 등)
- [선택] 시스템별 실패 시 처리
- [선택] 계약과 API 키 준비 상태, 테스트 환경 유무

### A.6 데이터 → `req/data/`

- [필수] 기존 데이터 이관 여부
- [선택] 보존 기간, 탈퇴 시 삭제 범위
- [선택] 백업 주기, 복구 목표 시간
- [선택] 통계·리포트 필요 여부

### A.7 제약 → SRS `05-constraints`

- [필수] 기술 스택
  → UI 기초(4.5)를 만들려면 먼저 정해져야 한다
- [필수] 호스팅·인프라
- [선택] 예산, 팀 규모
- [선택] 준수해야 할 법규 (개인정보보호법 등)
- [선택] 사용할 수 없는 기술

## 부록 B. 설계 문서별 필수 항목 (초안)

> 설계 스킬을 만들 때 템플릿으로 옮긴다 (4.1).
> 공통 규칙(frontmatter, 섹션 순서, 정본 참조)은 4.1을 따른다.

| 문서 | 필수 항목 |
|---|---|
| `architecture.md` | 구성도(mermaid), 구성 요소별 책임, 기술 스택(근거 `CON-`), 모듈 경계와 의존 방향, 배포 단위, 적용 NFR |
| `security.md` | 인증 흐름, 인가 모델(`req/actors.md` 참조), 토큰·세션 저장 방식, 암호화·마스킹 대상 필드, 감사 로그, 근거 `SEC-` |
| `database/erd.md` | ERD(mermaid), 테이블별 컬럼(이름·타입·null·기본값·설명), PK·FK·인덱스·유니크, 삭제 정책(soft/hard), 명명 규칙. 상태 컬럼은 state 문서 참조 |
| `api/_conventions.md` | API 공통 규약: 오류 응답 형식, 인증 헤더, 페이징, 버전 |
| `api/<domain>.md` | API별: ID, 메서드·경로, 근거 `FR`, 허용 역할, 요청·응답 스키마, 오류(코드·조건), 유발하는 상태 전이, 멱등성 |
| `integration/<system>.md` | 근거 `INT-`, 호출 방향(우리 → 외부 / 웹훅), 인증 방식, 타임아웃·재시도·멱등 키, 실패 처리와 보상, 테스트 환경 |
| `state/<entity>.md` | 4.6을 따른다 |
| `ui/ia.md` | 화면 목록(SCR ID, 이름, 경로, 접근 역할, 근거 `FR`), 메뉴 계층, 화면 흐름 |
| `ui/screens/<domain>/<screen>.html` | 맨 위 메타 주석(4.1), 기본·빈 상태·로딩·오류 화면 |

## 부록 C. CLAUDE.md 템플릿 (초안)

> 하네스 초기화 스킬을 만들 때 템플릿으로 옮긴다 (0.1).

````markdown
# <프로젝트명>

<한 줄 요약>. 상세는 docs/srs/01-overview.md

## 작업별 진입 경로
| 작업 | 먼저 읽을 문서 |
|---|---|
| 기획 (인터뷰, SRS, REQ) | docs/srs/README.md → docs/req/README.md |
| 설계 | 해당 FR 파일 → docs/design/architecture.md → 해당 도메인 설계 문서 |
| 구현 | 해당 FR 파일 → FR ID로 검색한 설계 문서 → docs/conventions.md |
| 검증 | 해당 FR 파일의 수용 기준 → records/verifications/ |
| 변경·결정 | records/open/, records/changes/, records/decisions/ |

## 로딩 원칙
- 인덱스(README.md)를 먼저 읽고, 필요한 파일만 읽는다
- docs/ 전체를 한꺼번에 읽지 않는다
- records/는 이유나 경위가 필요할 때만 읽는다

## 핵심 규칙
- MUST: 문서를 approved로 바꾸지 않는다. 승인은 사용자만 한다
- MUST: 하위 단계에서 상위 문서를 고치지 않는다. 해당 단계의 루프를 호출한다
- MUST: 승인된 내용을 바꿔야 하면 CR을 작성한다
- MUST: 모르는 것은 추측하지 않고 OPEN으로 등록한다
- MUST: 테스트를 통과시키려고 테스트를 고치지 않는다
- NEVER: 시크릿 값을 읽거나 출력하거나 커밋하지 않는다
- MUST: 커밋에 트레일러(Refs, Decision, CR, Closes, Role)를 단다

## 명령
| 용도 | 명령 |
|---|---|
| 개발 서버 | (프로젝트별 결정) |
| lint·타입 검사 | (프로젝트별 결정) |
| AC 테스트 | (프로젝트별 결정) |
| 설계 교차 검증 | (하네스 스크립트) |
````
